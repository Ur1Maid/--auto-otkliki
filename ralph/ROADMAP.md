# ROADMAP — источник истины для Ralph

> Это рабочий backlog автономного цикла Ralph. Цикл на КАЖДОЙ итерации читает этот файл,
> берёт **одну** верхнюю невыполненную задачу `[ ]` из активного майлстоуна, делает её,
> верифицирует, коммитит и отмечает `[x]`. Держи задачи маленькими (≈1 коммит каждая).
>
> Формат задачи: `- [ ] (ID) Заголовок — Acceptance: <как проверить, что готово>`
> Активный майлстоун помечен **▶ ACTIVE**. Когда все задачи майлстоуна `[x]`, сними метку
> ACTIVE, поставь её на следующий и продолжай. Когда всё `[x]` — выведи `RALPH: ALL DONE`.
>
> **Порядок = приоритет.** Сначала фундамент (M1–M2), затем продуктовые фичи (M3–M7),
> затем общий хардненинг (M8–M10), который страхует фичи.
>
> **Инварианты на весь backlog (проверяет orchestrator/security-reviewer):**
> отклики по 3 резюме одновременно — сохранить; honesty-гардрейлы DeepSeek — не ослаблять;
> ключ/сессии/PII — не логировать/не коммитить; реальный браузерный флоу — не гонять против
> реальных аккаунтов в тестах. DOM-задачи требуют живого HTML от пользователя (research-first):
> их acceptance = чистый парсер на сохранённом санитизированном фикстуре + ручная проверка.

---

## M0 — Verification harness (предусловие Ralph) ▶ DONE
- [x] (M0.1) `npm test` через `node --test`, smoke-тест `node --check` на всех `src/*.js`.
- [x] (M0.2) Ralph-скрипты, PROMPT, ROADMAP, PROGRESS, ветка `ralph/auto`.

## M1 — Извлечь чистую логику из `src/review.js` в тестируемые модули ▶ DONE
Цель: вынести функции без сайд-эффектов в `src/lib/*` с `export`, переиспользовать их в
`review.js` (импортом), покрыть unit-тестами. `review.js` исполняется при импорте — тестировать
можно только извлечённые модули. Каждая задача оставляет `npm test` зелёным и не меняет поведения.

- [x] (M1.1) `src/lib/text.js`: вынести `parseJsonObject`, `cleanGeneratedAnswer`; импортировать в `review.js`. `test/text.test.mjs`: fenced ```json```, JSON с префиксом, мусор → дефолт; снятие кавычек/`NO_ANSWER`/плейсхолдеров. — Acceptance: `npm test` зелёный, `node --check src/review.js` ок.
- [x] (M1.2) `src/lib/fields.js`: `detectFieldKind`, `isSalaryContext`, `isGenericFieldContext`, `getMainQuestion`. Тест: ветки salary/coverLetter/answer/unknown. — Acceptance: `npm test` зелёный.
- [x] (M1.3) `src/lib/knowledge.js`: `normalizeText`, `getSearchTerms`, `extractResumeKeywords`, `pickKnowledgeChunks`. Тест: границы слов, отбор top-N. — Acceptance: `npm test` зелёный.
- [x] (M1.4) `src/lib/urls.js`: `normalizeHhUrl`, `normalizeVacancyUrl`. Тест: валидные/невалидные vacancy URL, срез query/hash. — Acceptance: `npm test` зелёный.
- [x] (M1.5) `src/lib/answers.js`: `optionMatches`, `looksLikeEmployerVoice`, `matchesAnyPattern`. Тест: матчинг опций, детект employer-voice. — Acceptance: `npm test` зелёный.

## M2 — DeepSeek-клиент как модуль (фундамент для экономии токенов) ▶ DONE
- [x] (M2.1) Вынести `callDeepSeek` в `src/lib/deepseek.js` (сигнатура без изменений): timeout, `!ok`, 402. Тест с замоканным `fetch`. — Acceptance: `npm test` зелёный, ключ не логируется.
- [x] (M2.2) Ретрай с backoff на сетевые ошибки/429 (не на 402). Тест на число попыток. — Acceptance: тест зелёный.
- [x] (M2.3) Юнит-тест: `appendDeepSeekDebug` никогда не содержит `apiKey`/`Authorization`. — Acceptance: тест зелёный.

## M3 — Экономия токенов DeepSeek (фича #1: «тратит слишком много») 🎯 ▶ DONE (actionable; остаток M3.3 low-match reject — под code/security-review, не ACTIVE)
> РЕАКТИВИРОВАН пользователем (2026-07-01): пройтись по отложенным M3.x заново. Пользователь
> прислал живой SERP-HTML (`D:/workplace/hh_search_body.txt`) с плашкой «Подходит по навыкам на N%»
> и company-текстом → M3.3/M3.8 РАЗБЛОКИРОВАНЫ (селекторы верифицированы, задачи actionable);
> разрешил живой прогон с ключом → M3.12 РАЗБЛОКИРОВАН (замер, оператор/оркестратор). M3.5/M3.11
> остаются отложенными с обоснованием; M3.14 сделан. ЗАДАЧА РАЛФА: по каждой отложенной задаче свежий
> research-first и либо (a) реализовать чистым модулем+тестом, если блокер снят (напр. M3.14 —
> появился `config/accounts/*/preferences.txt` со структурой), либо (b) если блокер жив (нужен
> живой DOM карточек поиска / живой API / конфликт с 3-резюме-параллелью / риск на honesty) —
> обновить обоснование в строке задачи актуальным фактом и пометить `[x]` как «пересмотрено,
> остаётся отложенной: <причина>» (НЕ угадывать, НЕ ослаблять инварианты). Каждая итерация
> оставляет `npm test` зелёным.
**Инсайт по стоимости:** доминирует ВХОДНОЙ объём на скоринге. `scoreVacancyWithDeepSeek`
(review.js:720) на каждую вакансию шлёт полный текст вакансии (до 9000 симв., `getVacancyText`
review.js:356) + полное резюме, ×200 ×3 аккаунта ≈ 600 вызовов по ~4000 вход. токенов; вывод 120.
Поэтому бьём по трём осям: (A) число вызовов, (B) размер входа, (C) убрать модель где можно.
Инвариант: 3-резюме-параллель и все функции сохраняем; honesty-гардрейлы не трогаем. Каждая
задача — чистый модуль + тест; замеры экономии писать в `PROGRESS.md`.

### A. Меньше вызовов
- [x] (M3.1) ПЕРЕОПРЕДЕЛЕНО пользователем (2026-06-26): не skip-пре-фильтр, а ПРИОРИТИЗАЦИЯ без отсева. По присланному HTML поиска верифицированы селекторы: карточка `[data-qa="vacancy-serp__vacancy"]`, заголовок `a[data-qa="serp-item__title"]`, метка удалёнки `[data-qa="vacancy-label-work-schedule-remote"]`. `collectFromSearch` теперь собирает {url, remote} по карточкам (+ flat-fallback, ни одна вакансия не теряется) и возвращает в порядке удалёнка→остальные через чистый `src/lib/vacancyPriority.js: prioritizeRemoteFirst`. Проверено на реальном HTML: 50 вакансий, 21 удалёнка в начале. Acceptance: `npm test` зелёный (822), 7 юнит-тестов приоритизации.
- [x] (M3.2) 🔥 `src/lib/localScore.js`: локальная релевантность (overlap/TF-IDF по резюме). DeepSeek звать ТОЛЬКО для «серой зоны» (порог-окно, напр. 40–70); явные да/нет решаются локально. Тест на синтетике. — Acceptance: доля вакансий в DeepSeek заметно падает.
- [ ] (M3.3) РАЗБЛОКИРОВАНО живым SERP-HTML (2026-07-01, `D:/workplace/hh_search_body.txt`). EVIDENCE: плашка «Подходит по навыкам на N%» подтверждена ВНУТРИ карточки `[data-qa="vacancy-serp__vacancy"]` (опциональна — есть не у всех карточек; значения 71/80/85/100%). БЕЗ `data-qa` — только хешированный класс `magritte-tag__label___…`, поэтому селектор ТЕКСТОВЫЙ: regex `/Подходит по навыкам на\s*(\d+)%/` (nbsp между словами → `\s`, кириллица-safe без `\b`/`\w`, урок M11.6). ЗАДАЧА: расширить пер-карточный `page.evaluate` в `collectFromSearch` (review.js) — вынимать `matchPercent` (0..100|null) по каждой карточке. Использование СТРОГО по инварианту M3.2 (локальное только ОТКЛОНЯЕТ, НИКОГДА не пропускает к отклику без модели): (a) приоритизация как remote-first M3.1 — выше matchPercent раньше; (b) БЕЗОПАСНЫЙ пре-reject — очень низкий matchPercent (порог, напр. `<30`) → skip DeepSeek-скоринга (0 токенов) как уверенный reject (это НЕ пропуск-к-отклику). Чистый `src/lib/matchBadge.js` (`parseMatchPercent(text)` + `isConfidentMatchReject(pct, threshold)` + интеграция в приоритет) + тест на синтетике/мини-фикстуре; проводка в evaluate — resilient. Honesty/safety не ослаблять (reject≠apply). — Acceptance: matchPercent извлекается из nbsp-текста, низкий → 0-токенов reject, порядок сохраняет удалёнку+высокий-матч; `npm test` зелёный. ЧАСТИЧНО СДЕЛАНО (2026-07-01, оркестратор): `src/lib/matchBadge.js` (parseMatchPercent nbsp-safe + isConfidentMatchReject, консервативный порог 20, +9 тестов) готов; `loadSearchPage` вынимает текст плашки, `collectFromSearch` парсит → `matchPercent`; `prioritizeRemoteFirst` расширен вторичным ключом «выше match раньше» внутри remote/не-remote групп (backward-compat: без плашки — DOM-порядок; +4 теста). ОСТАЛОСЬ (для ралфа под code/security-review): провести БЕЗОПАСНЫЙ low-match pre-reject (`isConfidentMatchReject`) в скоринг-путь reviewVacancy (только ОТКЛОНЯЕТ, thread `matchPercent` как `remoteSet`) — поведенческое изменение apply-флоу, требует ревью-гейта; оркестратор его НЕ трогал (ралф rate-limited).
- [x] (M3.4) ◐ Батч-скоринг `scoreVacanciesBatch` в `deepseek.js`: N вакансий (заголовок+усечённый срез) одним вызовом → `[{id,score,reason}]`, кламп каждого, парс через `parseJsonObject`. Тест с замоканным `fetch`. — Acceptance: один вызов на N. (Реализовано как `src/lib/batchScore.js`, standalone-блок; во флоу review.js не проведён — интерактивный флоу по одной вакансии; батч-пред-пасс будет в демоне M7.)
- [x] (M3.5) ПЕРЕСМОТРЕНО (2026-07-01), остаётся отложенной: блокер жив, revisit-условие не выполнено. (1) Кросс-аккаунтный конфликт СОХРАНЯЕТСЯ: `processAccount` по каждому аккаунту заворачивается в `Promise.all` (review.js:2011-2013, autoApply) — независимые процессы/браузеры, каждый со своим `scoreCache`; общий модельный gate требовал бы кросс-аккаунтной синхронизации, ломающей эту изоляцию. (2) Revisit-условие «батч-пред-пасс демона (M7) с общим пулом» НЕ наступило: `runApplyPass` (daemon.js:110-134) просто спавнит один `review.js` со всеми аккаунтами и делегирует обратно в тот же параллельно-независимый дизайн — общего пула вакансий нет; `batchScore.js` (M3.4) остаётся standalone, во флоу не проведён. (3) Выгода маргинальна: доминирующий junk уже отсекается локальным reject БЕЗ модели (0 токенов, review.js:1592-1608 `localRelevanceScore`+`needsModelScoring`); общий gate сэкономил бы лишь на borderline-выживших (1 вызов вместо 3) — недостаточно, чтобы оправдать слом аккаунт-изоляции. Разблокировать = переход на демон-батч-пред-пасс с общим пулом вакансий (не угадывать, не ослаблять 3-резюме-инвариант).
- [x] (M3.6) ◐ `src/lib/scoreCache.js` → `data/score-cache.json` (git-ignored) по `vacancyId`+хэш резюме; инвалидация при смене резюме. Тест hit/miss. — Acceptance: повторный прогон неизменных = 0 новых вызовов; путь в `.gitignore`.
- [x] (M3.7) ◐ Проверять «уже откликнулись»/«отклик отправлен» (`APPLIED_PATTERNS`) ДО скоринга — не тратить токены на недоступное. — Acceptance: applied-вакансии скоринг не вызывают.
- [x] (M3.8) РЕАЛИЗОВАНО (2026-07-01, оркестратор — ралф был rate-limited). EVIDENCE: company-селектор `[data-qa="vacancy-serp__vacancy-employer-text"]` (50/50 карточек, «Volna.tech»). `loadSearchPage` (review.js) теперь вынимает `company`+`title` по карточке; `src/lib/vacancyDedup.js: dedupeReposts(cards)` схлопывает репосты с одинаковым нормализованным `title+company` (разные id/url), сохраняя ПЕРВУЮ; пустая company/title → НЕ схлопывает (консервативно); битые элементы пропускаются. Проведён в `collectFromSearch` перед приоритизацией (лог «репостов схлопнуто N», без PII). +7 тестов. Acceptance выполнен: репост схлопывается, разные компании с одним title не сливаются, пустая company не режет; `npm test` 1290 зелёный. ЗАМЕТКА: сделано без code/security-reviewer (ралф rate-limited до 06:40 Екб) — только удаляет точные дубли (никогда не создаёт отклик); ревью-пасс желателен при следующем разблокe ралфа.

### B. Меньше входа на вызов
- [x] (M3.9) 🔥 `src/lib/vacancyExtract.js`: усечь текст для скоринга 9000→~1500 симв., вынимая блок «Требования/Обязанности» и выкидывая бойлерплейт (о компании/льготы/юр.). Тест на фикстурах. — Acceptance: вход скоринга кратно меньше, релевантность сохранена.
- [x] (M3.10) 🔥 «Профиль резюме»: один раз на аккаунт строить короткую выжимку (роль + список навыков) и слать её в скоринг вместо полного `resume.md`. Чистая `buildResumeProfile`. Тест. — Acceptance: скоринг использует профиль, не полный текст.
- [x] (M3.11) ПЕРЕСМОТРЕНО (2026-07-01), остаётся отложенной: revisit-условие «форменные вызовы стали заметной статьёй» НЕ выполнено; risk/reward без изменений. (1) МАЛО ВЫЗОВОВ: `askDeepSeekForm` (review.js:1321) достижим только через `completeApplicationFlow` (review.js:1681) ПОСЛЕ прохождения порога (`relevance.score < deepSeekContext.minScore` → skip, review.js:1626-1628; дефолт minScore=65, review.js:51) и dry-run-гарда (review.js:1633) — т.е. лишь ~10-30 из 200 вакансий. Доминанта стоимости (M3 header:52-55) — ВХОД скоринга (~600 вызовов ×~4000 ток.), не форма. (2) РИСК НА HONESTY: форменный вызов = исходящее действие (сопроводительное/вопросы работодателя); в user-промпт идёт ПОЛНЫЙ `resume` (review.js:1358-1359), не профиль M3.10 — усечение именно здесь обеднит данные там, где honesty-гардрейл (review.js:1349-1352 «не выдумывай…», «если нельзя честно — не добавляй») требует полноты, ради маргинальной экономии на десятках вызовов. Дешёвые оси уже закрыты БЕЗ риска: RAG ужат/скоуплен по kind (M3.15, review.js:1331-1333), сопроводительное только когда обязательно (M3.13, review.js:1008 `coverLetterRequired`), salary — только при `needsSalary` (review.js:1332,1385). Разблокировать = замер (M3.16-счётчик), показавший форму заметной статьёй токенов, + безопасный способ усечь без потери honesty-данных (не угадывать, не ослаблять гардрейлы).

### C. Убрать модель / инфраструктура
- [x] (M3.12) ЗАМЕРЕНО НА ЖИВОМ ПРОГОНЕ (2026-07-01, оркестратор, ключ разрешён пользователем). БЕЗОПАСНЫЙ `--dry-run` прогон (startsev, `--text DevOps --area 1 --limit 3`): полный скоринг реальными вызовами DeepSeek, 0 сабмитов. РЕЗУЛЬТАТ: `вызовов 4, вход 14603 токенов, выход 265, всего 14868, из них cache-hit 6144` → **~42% входных токенов обслужено context-cache DeepSeek** на стабильном префиксе (резюме первым). Первый вызов праймит кэш, вызовы 2-4 попадают — cache-hit>0 подтверждён ЗАМЕРОМ (2-й прогон не нужен, эффект виден внутри прогона). Реализуемая часть (стабильный префикс + счётчик `prompt_cache_hit_tokens`, M3.16) работает как задумано. Acceptance выполнен: cache-hit замерен и зафиксирован числами. ЗАМЕТКА: belonogov в том же прогоне завис на сборе (>30с, session-flagged/captcha — специфично для аккаунта, startsev чист) → нужен re-login belonogov (follow-up M18).
- [x] (M3.13) 🔥 Сопроводительное только когда обязательно: `coverLetterRequired(form, pageText)` И только для вакансий выше высокого порога. Тест на фикстурах форм. — Acceptance: опциональные письма = 0 токенов.
- [x] (M3.14) РЕАЛИЗОВАНО (2026-07-01, блокер снят): revisit-условие «structured prefs» выполнено — появился `config/accounts/*/preferences.txt` со структурой «ключ: значение» (пример: `config/accounts/example/preferences.example.txt`, готовность к переезду/командировки/тип занятости/формат/график/локация/гражданство/…). Больше НЕ угадывание: чистый `src/lib/preferenceAnswer.js` (`parsePreferences`/`findPreferenceCategory`/`answerFromPreferences`) возвращает на типовые вопросы работодателя ТОЛЬКО явно указанное кандидатом значение; пустое/неизвестное/нераспознанное → `null` (вызывающий: модель или NO_ANSWER) — honesty сохранён, никакой фабрикации. 10 категорий, кириллица-safe regex (без `\b`/`\w`, урок M11.6), never-throws. `answerFromPreferences` звать вместо DeepSeek на переезд/занятость/командировки/формат/график/локацию → 0 токенов на эти поля. 44 юнит-теста (дизамбигуация переезд↔«за рубеж», защита `Релокация`↔`Локация`, мусорный массив). Проводка в live-форму `askDeepSeekForm` (review.js) = gated follow-up (исходящее действие, дефолт dry-run), как M3.4/M3.6. Acceptance: `npm test` зелёный (1271).
- [x] (M3.15) ◐ Ужать RAG: чанков 3→2, короче срез, прикладывать только к kind, которым нужно (не salary/короткие). Тест лимитов. — Acceptance: меньше токенов, поведение сохранено.

### Замер
- [x] (M3.16) Счётчик токенов/вызовов за прогон (вход/выход, cache-hit) в summary; зафиксировать «до/после» в `PROGRESS.md`. — Acceptance: видно суммарное снижение.

## M4 — Авто-ответы на сообщения hh.ru (фича: отвечать на все сообщения)
Новый модуль `src/messages.js`. Untrusted-вход (письма работодателей) → prompt-injection: гардрейлы
сохраняют авторитет, текст письма никогда не становится селектором/командой.
- [x] (M4.1) research-first: пользователь даёт HTML страницы «Сообщения»/переговоров. Зафиксировать селекторы в `src/lib/selectors.js`, создать скелет `src/messages.js`. — Acceptance: парсится; evidence в PROGRESS.
- [x] (M4.2) Детект непрочитанных тредов (text/role-based, индикатор непрочитанного). Чистый парсер на фикстуре. — Acceptance: различает unread/read на фикстуре.
- [x] (M4.3) Чтение треда + извлечение последнего сообщения работодателя (санитизация untrusted). Тест на фикстуре. — Acceptance: извлечение корректно.
- [x] (M4.4) `src/lib/replyPolicy.js`: локальные правила «нужен ли ответ» (системные/авто-сообщения, «отклик просмотрен», приглашения vs вопросы) → DeepSeek зовём ТОЛЬКО на настоящие вопросы. Тест правил. — Acceptance: для no-reply кейсов 0 токенов.
- [x] (M4.5) Генерация ответа через DeepSeek только когда нужно: candidate-voice, honesty, `NO_ANSWER`→пометить на ручную, длина ограничена. Тест с замоканным `fetch`. — Acceptance: ответ только для тредов-вопросов.
- [x] (M4.6) Отправка ответа: по умолчанию `--reply-manual` (подтверждение, т.к. исходящее действие наружу); `--reply-auto` — без подтверждения. Идемпотентность: помечать обработанные треды, без дублей. — Acceptance: нет двойных ответов; дефолт требует подтверждения. РЕШЕНИЕ (2026-06-25): идемпотентность ЕСТЕСТВЕННАЯ — опираемся на replyPolicy (после отправки последнее сообщение в треде наше → следующий поллинг такой тред skip-ает) + лёгкий in-memory set обработанных за сессию; БЕЗ файла состояния.
- [x] (M4.7) 15-мин поллинг в демоне: refresh страницы → скан непрочитанных → обработка. — Acceptance: цикл с интервалом, останавливается по расписанию/STOP. [Тестируемое ядро `src/lib/pollLoop.js` (runPollingLoop, интервал/стоп/изоляция/maxIter) + браузерная оркестрация `processUnread` в messages.js (dry-run дефолт, handshake трекера, изоляция тредов); реальный запуск демона — за подтверждением.]

## M5 — Микро-правки резюме для очков активности (фича: каждые 30 мин)
Чувствительно (правим реальный PII-файл) → канонический бэкап + обратимость + лимит дивергенции.
- [x] (M5.1) research-first: предпочесть нативную кнопку hh.ru «обновить дату/поднять резюме» (активность без изменения текста, есть кулдаун). Зафиксировать селекторы. — Acceptance: evidence в PROGRESS. [Живой HTML резюме hh.ru от пользователя: data-qa="resume-update-button", активный лейбл «Поднять в поиске», кулдаун «Обновить можно через {время}», canTouch-флаг. Селекторы в selectors.js, src/lib/resumeBump.js + 24 теста.]
- [x] (M5.2) Если нативной кнопки нет: `src/lib/microEdit.js` — обратимая правка безопасного поля (whitespace/синоним) с мгновенным ре-сейвом; канонический снимок сохраняется, дифф минимален. Чистые `applyMicroEdit`/`revert`. Тест обратимости. — Acceptance: правка обратима, канон сохранён.
- [x] (M5.3) Кулдаун/рейт-лимит на аккаунт (cadence 30 мин, уважать кулдаун hh.ru). Чистый планировщик. Тест. — Acceptance: не чаще допустимого.
- [x] (M5.4) Интеграция в 30-мин цикл демона + лог активности. — Acceptance: цикл идёт, события логируются. [daemon.js runMicroEditPass: навигация на /applicant/resumes + bumpResume (dry-run наследуется), изоляция аккаунта, browser.close в finally, report.recordResumeEdit(applied=bumped); кулдаун hh.ru детектится сам → дёшево вызывать каждые 30 мин.]

## M6 — Тейлоринг резюме после прогона 200 откликов (фича: подгонять под вакансии, не уходя далеко)
- [x] (M6.1) Адаптировать существующий `buildResumeUpgradeReport`/коллектор, чтобы возвращал машинно-применимые предложения (additive skills/фразы с обоснованием). Тест структуры. — Acceptance: structured suggestions + тест.
- [x] (M6.2) `src/lib/resumeTailor.js`: применять только additive, обоснованные правки; лимит дивергенции (≤ N изменённых строк / similarity ≥ порога к оригиналу) + timestamped-бэкап `resume.md`. Чистые функции диффа/лимита. Тест порога. — Acceptance: правки в пределах лимита, бэкап создан, honesty сохранён (никаких выдуманных навыков). [Чистые функции готовы; реальная запись файла + бэкап — M6.3, за подтверждением.]
- [x] (M6.3) Привязка к концу apply-прогона (после 200 откликов). — Acceptance: после прогона resume.md получает ограниченные правки + бэкап + отчёт. [Safe writer `src/lib/resumeWriter.js` (dry-run дефолт, бэкап-до-перезаписи, fs-инъекция) + привязка в finishResumeUpgradeReport: отчёт получает машинно-применимые предложения (M6.1). Реальная запись resume.md = operator-gated (явные approvedSkills + dryRun:false), т.к. honesty запрещает авто-добавление неодобренных навыков.]

## M7 — Дневной демон + автозапуск 09:00–18:00 МСК (фичи: работа весь день + расписание)
- [x] (M7.1) `src/lib/schedule.js` (чистые функции МСК независимо от TZ ПК): `isWithinWorkingHours(now)`, `nextRunAt(...)` для Europe/Moscow. Тест на фиксированных датах. — Acceptance: детерминированно, тест зелёный.
- [x] (M7.2) `src/daemon.js` (или `--daemon`): на старте — прогон 200 откликов по 3 аккаунтам → тейлоринг резюме (M6) → далее переплетённые циклы: сообщения каждые 15 мин (M4), микро-правки каждые 30 мин (M5); до 18:00 МСК. Чистый «планировщик решений» (что запускать в момент T) — тест. — Acceptance: решения планировщика покрыты тестом. [Чистый планировщик `src/lib/daemonPlan.js` готов+покрыт; реальный цикл `src/daemon.js` (исполнение наружу) — за подтверждением.]
- [x] (M7.3) Устойчивость: STOP-файл/Ctrl-C, лог на итерацию, изоляция ошибок (одна вакансия/тред не роняет весь день). Тест изоляции. — Acceptance: ошибка локализуется. [Чистый примитив `src/lib/isolate.js` (runIsolated/runIsolatedTask/isStopRequested) готов+покрыт; фактический STOP-файл/обработчик сигналов/лог-на-итерацию — IO живого демона, за подтверждением.]
- [x] (M7.4) `scripts/register-task.ps1` + `scripts/unregister-task.ps1`: Windows Task Scheduler, ежедневный запуск `npm run daemon` в 09:00 (документировать МСК/таймзону). — Acceptance: задача регистрируется (проверить `schtasks /query` в ревью; авто-запуск не дёргаем). [Скрипты написаны, parse-OK, UTF-8 BOM (PS 5.1); npm-скрипт daemon добавлен; авто-запуск не дёргали.]
- [x] (M7.5) README демона + safety-доку (что делает наружу, как остановить). — Acceptance: доки на месте. [docs/daemon.md: что наружу/гарды/флаги/как остановить (DAEMON_STOP+Ctrl-C)/автозапуск/ограничения.]

## M8 — Hardening Playwright-флоу (страхует M3–M7) ▶ DONE
> Прыгаем сюда после M3: M4.6/M4.7 (отправка/демон) и M5/M6/M7 (правка реального PII-резюме,
> дневной демон) — это граница «действие наружу / правка реального PII», требует подтверждения
> пользователя перед реализацией. M8/M9/M10 — чистый хардненинг/наблюдаемость/CI без выхода наружу.
>
> СТАТУС (2026-06-26): Чистые блоки + execution-слой за safe-гардами СДЕЛАНЫ. Выбор пользователя
> «писать код за safe-гардами» выполнен: M4.6 (отправка), M4.7 (поллинг+оркестрация), M6.3 (запись
> resume.md), M7.2-демон (src/daemon.js), M7.4 (Task Scheduler), M7.5 (docs/daemon.md) — все с
> дефолтом dry-run, никогда не запускались против реальных аккаунтов. Тесты 76→791, зелёные.
> M5.1/M5.4 ЗАКРЫТЫ (2026-06-26): пользователь прислал живой HTML резюме → нативная кнопка
> «Поднять в поиске» (data-qa="resume-update-button") зафиксирована, resumeBump.js проведён в
> MICRO_EDIT-шаг демона (dry-run дефолт). ВСЕ actionable-задачи роадмапа ВЫПОЛНЕНЫ.
> ОСТАЛИСЬ ТОЛЬКО M3-отсрочки (M3.1/3.3/3.5/3.8/3.11/3.12/3.14) — с обоснованием в строках
> задач (заблокированы живым DOM/API карточек поиска либо маргинальны/рискованны под инвариантами).
- [x] (M8.1) Единый реестр селекторов/паттернов в `src/lib/selectors.js` (`RESPONSE_BUTTON_TEXTS`, `APPLICATION_FLOW_BUTTON_TEXTS`, `APPLIED_PATTERNS`, `REQUIRED_MANUAL_PATTERNS`). — Acceptance: `npm test` зелёный, флоу не изменился.
- [x] (M8.2) `--dry-run`: полный разбор/скоринг, но НИКОГДА не кликать submit. Тест флага + явный гард перед сабмитом. — Acceptance: тест зелёный.
- [x] (M8.3) Гарантия закрытия browser/context на всех путях ошибок (демон работает часами). Ревью + ручная проверка. — Acceptance: нет утечек контекста.

## M9 — Наблюдаемость
- [x] (M9.1) Структурный summary в конце прогона аккаунта (просмотрено/отклики/пропуски/локально-пропущено/ошибки/токены) + `logs/summary-<account>.json`. — Acceptance: `npm test` зелёный, summary появляется.
- [x] (M9.2) Дневной отчёт демона (откликов/сообщений-обработано/правок-резюме/токенов за день) → `logs/daily-<date>.json`. — Acceptance: тест агрегатора зелёный. [Чистый агрегатор `src/lib/dailyReport.js` (createDailyReport + dailyReportFileName) готов+покрыт; запись файла — живой демон, за подтверждением.]

## M10 — Конфиг и CI
- [x] (M10.1) `src/lib/validateConfig.js` + тест: понятная ошибка при пустом `.env`/незаполненном resume. — Acceptance: тест зелёный.
- [x] (M10.2) GitHub Actions `ci.yml`: `npm ci` + `npm test` на push/PR (без Playwright-браузеров). — Acceptance: YAML валиден (проверка в ревью).

## M11 — Панель управления: запуск и отслеживание откликов / сообщений / резюме ▶ DONE
Боль (2026-06-29): дашборд — read-only агрегатор за всю историю. Нужна **панель управления**, через
которую оператор: (1) ЗАПУСКАЕТ по выбранному аккаунту любую из трёх задач — отклики, ответы на
сообщения, правки резюме; (2) в реальном времени ОТСЛЕЖИВАЕТ их — текущий шаг/прогресс, ресурсы
(RSS/CPU), токены и оценка стоимости DeepSeek за сегодня, индикатор живости; (3) видит капчу/антибот
и зависание сразу (урок 2026-06-29: belonogov словил «Подтвердите, что вы не робот» на 529-й
вакансии — в логе это всплыло постфактум). Расширяем существующий `src/dashboard.js` до
управляющей панели (локальный сервер 127.0.0.1), не ломая текущие агрегаты.
**Инвариант (УСИЛЕННЫЙ — панель ЗАПУСКАЕТ действия наружу):** сервер слушает только `127.0.0.1`,
без внешнего bind; live-действия — явный opt-in в UI (тумблер Dry-run→Live) + подтверждение, дефолт
**dry-run**; одна задача на аккаунт одновременно (никаких дублей-процессов); запуск/стоп идут через
существующий `daemon.js` (`--task`, STOP-файл/kill PID) — НЕ плодим новый путь действий наружу;
ключ/PII/тексты писем не отдаём в UI и не логируем (только числа/шаги/счётчики); новые файлы
статуса/ресурсов — git-ignored; каждая чистая часть — модуль + тест (ресурсы/состояние через
инъекцию, без реального `process` в тестах); **обязателен security-reviewer** (сетевая поверхность +
запуск live). Honesty-гардрейлы и 3-резюме-инвариант — не трогаем.

### A. Субстрат наблюдаемости (данные для панели)
- [x] (M11.1) `src/lib/heartbeat.js`: чистые `buildHeartbeat({task, account, phase, index, total, lastEvent, state, ts})` и `isStale(heartbeat, now, thresholdMs)`. Тест: формирование записи, свежий/устаревший хартбит, guard на не-объект/null. — Acceptance: `npm test` зелёный.
- [x] (M11.2) Писать heartbeat из живого флоу: `review.js` и `daemon.js` на каждом шаге/итерации перезаписывают `logs/status/<account>.json` (task, шаг, index/total, lastEvent, ts) через `heartbeat.js`. Путь в `.gitignore`. — Acceptance: файл обновляется по ходу прогона; секретов/PII нет; `node --check` на обоих ок. [Тонкая IO-обёртка `src/lib/statusWriter.js: writeHeartbeatFile` (best-effort, DI, 5 тестов) над чистым buildHeartbeat. review.js пишет старт/прогресс(index+result.status)/ошибка/done в цикле вакансий; daemon.js — старт/done в messages+resume шагах (apply пишет дочерний review.js). lastEvent — только статус-энам/литералы, без PII. `logs/status/` в .gitignore. code-review APPROVE, security PASS.]
- [x] (M11.3) `src/lib/resourceSample.js`: чистый `sampleProcessResources({memoryUsage, cpuUsage, prevCpu, elapsedMs})` → `{rssMb, heapMb, cpuPercent}` + `formatResources(...)`. Тест на инъецированных значениях (без реального `process`). — Acceptance: тест зелёный.
- [x] (M11.4) Семплинг ресурсов в демоне: раз в итерацию писать строку в `logs/resources.jsonl` (`ts, rssMb, heapMb, cpuPercent, openContexts`) через `resourceSample.js`. Путь в `.gitignore`. — Acceptance: jsonl растёт, поля корректны, без секретов.
- [x] (M11.5) Живой счётчик токенов/стоимости «за сегодня»: дать `runUsageCounter`/summary публичный снимок текущего накопителя (вход/выход/cache-hit/оценка $) без завершения прогона. Чистый агрегатор + тест. — Acceptance: снимок читается в любой момент прогона, тест зелёный. [`usageCounter.liveSnapshot(pricing)` → `{...snapshot(), estimatedCostUsd}`, делегирует оценку $ в `metrics.estimateCost` (единый источник цен, без price-drift); не мутирует состояние, never-throws. 5 тестов (делегирование, custom pricing, нули, идемпотентность чтения). code-reviewer APPROVE.]
- [x] (M11.6) `src/lib/runState.js`: чистые `detectAntiBot(pageTextOrSignals)` (капча/«подтвердите, что вы не робот») и `detectStalledRun(heartbeat, now, thresholdMs, withinWorkingHours)` → state `captcha`/`stalled`/`ok`; пишутся в heartbeat и warn в `logs/alerts.jsonl` (через `alerts.js`). Тест ветвей. — Acceptance: капча/stale-в-окне → warn-state; свежий/вне окна → ok. [Чистый модуль + `resolveRunState` (captcha>stalled>ok) + 20 тестов. Проводка state→heartbeat + warn→alerts.jsonl из живого review.js/daemon.js — gated follow-up (как M11.2/M11.4 для M11.1/M11.3).]

### B. Управление из панели (запуск / стоп)
- [x] (M11.7) `src/lib/taskControl.js`: чистые `buildTaskCommand({task, account, live, limit, text, area})` → argv для `daemon.js --task <apply|messages|resume>` (без live → dry-run-дефолт) и `canStart(runningTasks, account, task)` (одна задача на аккаунт). Тест маппинга + отказа на дубль. — Acceptance: argv корректны, live только по флагу, дубль отклонён. [Чистый модуль: `buildTaskCommand` (алиасы poll/bump/micro-edit как в daemonArgs, `--live` ТОЛЬКО при live===true, limit/text/area только для apply, limit→floor целого) + `canStart` (одна задача на аккаунт, защита от не-массива/мусора/плохого account/task). 65 тестов, argv верифицирован на round-trip через parseDaemonArgs (code-reviewer APPROVE). npm test 1015 зелёных.]
- [x] (M11.8) Управляющий слой в `dashboard.js`: `POST /api/start` спавнит `node src/daemon.js --task … --account … [--live]` (через `taskControl.js`), трекает PID, дефолт dry-run; `POST /api/stop` — STOP-файл/kill трекаемого PID. Слушать только `127.0.0.1`. — Acceptance: задача стартует и останавливается из UI; без live — dry-run; повторный старт той же задачи/аккаунта отклоняется (409). [src/lib/taskRunner.js: createTaskRunner(DI spawn/kill/now) — реестр задач по аккаунту над чистым taskControl.js (buildTaskCommand/canStart): дубль→409, spawn-сбой→500 без занятия аккаунта, exit/close/error освобождают аккаунт. dashboard.js: POST /api/start|/api/stop + GET /api/tasks, readJsonBody(64KB cap), createServer(deps) с инъекцией runner, bind только 127.0.0.1. Безопасность: live ТОЛЬКО при body.live===true; argv массивом в spawn (без shell); ответы/логи = только account/task/pid/live (без ключа/PII); +loopback-guard isLoopbackRequest (Host+Origin) против cross-origin POST/DNS-rebinding. code-reviewer APPROVE w/nits (закрыт misleading LIVE-лог + double-reject), security-reviewer PASS 0 crit/high (закрыт MEDIUM CSRF/Origin). +26 тестов. npm test 1041.]
- [x] (M11.9) Безопасность управления: live-запуск требует явного `live:true` в теле + лог «LIVE запущен оператором: <task>/<account>» (без секретов/PII); подтвердить, что bind только loopback. **security-reviewer**. — Acceptance: security-reviewer PASS; dry-run дефолт; нет внешнего bind; в логах нет ключа/писем. [Аудит-лог был мёртв в проде: createServer звал createTaskRunner() без log → дефолт no-op (taskRunner.js:40). Проведён реальный логгер console.log (конвенция проекта, dashboard.js:360) → строка «LIVE запущен оператором: <task>/<account>» теперь реально пишется в stdout (только task/account/live/pid, без ключа/PII/text). Остальные инварианты M11.9 уже стояли с M11.8: live строго live===true (3 слоя), bind только 127.0.0.1 + isLoopbackRequest, spawn argv-массивом без shell. security-reviewer PASS (0 crit/high, 2 инфо-Low); code-reviewer APPROVE. +2 теста (LIVE-аудит присутствует / dry-run не помечается LIVE). npm test 1041→1043.]

### C. UI панели
- [x] (M11.10) Блок «Управление» в дашборде: по каждому аккаунту три действия (Отклики / Сообщения / Резюме) со статусом idle/running/stopped, тумблер **Dry-run↔Live** с подтверждением на Live и кнопкой Стоп; дёргает `/api/start`/`/api/stop`. — Acceptance: кнопки запускают/останавливают задачи, Live требует подтверждения, статус обновляется.
- [x] (M11.11) Блок «Сейчас» в дашборде: по каждому аккаунту текущая задача/шаг/прогресс, индикатор живости (зелёный «работает» / красный «завис» / оранжевый «капча»), последние N событий, RSS/CPU, токены и оценка $ за сегодня; читает `logs/status/*` + `logs/resources.jsonl` + heartbeat; автообновление. Существующие агрегаты не ломать. — Acceptance: панель показывает живое состояние, на капче/зависании — явная пометка, агрегаты на месте. [Чистый `src/lib/liveStatus.js` (buildLiveView/accountLiveness: captcha>done→idle>stalled-в-окне>working, progressPct, sanitizeEvent→только {status,at}) + 17 тестов. dashboard.js: collectLive() (хартбиты logs/status/*.json + resources.jsonl + per-account responses-лог через getAccountLogPath) + GET /api/live (read-only, без loopback-гарда как /api/metrics) + UI-блок «Сейчас» (индикатор-точки, прогресс-бар, ресурсы, токены/$ из metrics, автообновление 5с). PII: title/url вакансий НЕ отдаём (whitelist {status,at}, тест-enforced). security-reviewer PASS (0 crit/high/med), code-reviewer APPROVE. npm test 1063 зелёных (+20).]

## M12 — Панель: фокус на ТЕКУЩЕМ прогоне + параллельный запуск трёх задач ▶ DONE
Боль (2026-06-30, от пользователя): (1) панель показывает статистику за всю историю — а нужно
ТОЛЬКО текущий прогон: что программа делает сейчас (собирает вакансии / откликается / умерла на
чём-то / почему ошибки); агрегаты за всё время — шум. (2) Сейчас жёсткое «одна задача на аккаунт»
не даёт запустить отклики + сообщения + резюме ОДНОВРЕМЕННО — а это нужно.
**Инвариант:** сохранить всю безопасность M11 (bind только `127.0.0.1` + loopback-guard; live —
явный per-task opt-in + подтверждение, дефолт dry-run; ключ/PII/тексты писем не отдавать/не
логировать); параллельные задачи = независимые процессы, каждый со своим браузером → изоляция и
закрытие контекста на всех путях; honesty-гардрейлы и 3-резюме — не трогать; чистые части —
модуль + тест; **security-reviewer обязателен** на изменения конкурентности (taskRunner) и spawn.

### A. Фокус на текущем прогоне (что делает программа сейчас)
- [x] (M12.1) Богатый heartbeat по фазам из `review.js`: `phase` ∈ `collecting` (собирает вакансии: собрано N) / `scoring` (оценивает i/total) / `applying` (откликается i/total) / `done` / `error`; при ошибке — короткая причина (енам/литерал, БЕЗ PII/секретов/текста вакансии). Чистый маппер фазы + тест. — Acceptance: status-файл отражает текущую фазу и причину ошибки; PII не утекает (тест).
- [x] (M12.2) Блок «Сейчас» показывает фазу СЛОВАМИ: «Собирает вакансии: 250», «Откликается 12/40», «Ошибка: <причина>», «Капча», «Готово». Чистый формат-хелпер `formatPhase` + тест. — Acceptance: видно текущее действие человекочитаемо. [Чистый `formatPhase(snapshot)` в runPhase.js (captcha>фаза; collecting/scoring/applying/done/error→русские фразы; ошибки маппятся в фикс-метки REASON_LABELS, raw lastEvent НЕ эхо-ится — PII-safe, тест); проведён через liveStatus.buildLiveAccount→`phaseLabel`; dashboard рендерит a.phaseLabel + убран дубль прогресса из live-meta. +11 тестов. code-reviewer APPROVE WITH NITS (нит-фиксы внесены). npm test 1087 зелёных.]
- [x] (M12.3) Счётчики ТЕКУЩЕГО прогона (не за всё время): отправлено/пропущено/ошибок/токенов за ЭТОТ запуск (из heartbeat/summary активного прогона, не из накопленных логов). Чистый агрегатор + тест. — Acceptance: цифры относятся к активному прогону, обнуляются на новый запуск. [Чистый `src/lib/runCounters.js: buildRunCounters({summary, tokens})` — проекция снимка runSummary.snapshot() + usageCounter.snapshot()/liveSnapshot() в фокус-счётчики панели: sent=applied+dryRun (зеркалит review.js appliedSoFar), skipped/manual/alreadyApplied/errors/viewed, tokens{calls,prompt,completion,totalTokens(пересчёт),cacheHit,estimatedCostUsd(passthrough, цены не считаем — единый источник metrics.estimateCost)}. «Обнуляется на новый запуск» т.к. источник — свежий per-run инстанс; пустой snapshot→нули (тест). Never-throw, без NaN-утечек, без IO/PII. +19 тестов. code-reviewer APPROVE WITH NITS (нит-доку закрыт). Проводка в dashboard — M12.4/M12.6. npm test 1106 зелёных.]
- [x] (M12.4) Переориентировать дашборд на «сейчас»: блоки «Управление» + «Сейчас» — наверху и в фокусе; агрегатные графики за всю историю убрать со старта (свернуть под раскрывающийся блок «История» или отдельный путь `/history`). — Acceptance: дефолтный экран — текущий прогон, история не мешает и доступна по запросу.

### B. Параллельный запуск трёх задач на одном аккаунте
- [x] (M12.5) `taskControl.canStart` + `taskRunner`: ключ реестра `account+task` вместо `account` → разрешить `apply`+`messages`+`resume` ОДНОВРЕМЕННО на одном аккаунте; дубль той же пары `task+account` по-прежнему 409. Тест конкурентности. — Acceptance: три задачи на один аккаунт стартуют параллельно; повтор той же пары отклонён. [Чистый слой: `canStart` теперь pair-aware (нормализует обе стороны через `normalizeTask` → алиас poll/bump не обходит 409); `taskRunner.running` re-keyed на составной `keyOf(account,task)` (NUL-разделитель), cleanup-closure сверяет identity по составному ключу; `stop({account,task?})` — task опционален и тоже нормализуется (алиасы; unknown→400), без task → стоп всех задач аккаунта (1→`{task}`, N→`{stopped:[]}`). security-reviewer PASS (0 crit/high/med; 1 LOW операционная заметка: параллельные live-дети одного аккаунта делят `.hh-session/<acc>/storage-state.json` и `data/score-cache.json` — пре-existing дизайн, теперь обнажён снятием сериализации; разрулить = M12.6+ / отдельная задача). code-reviewer: backend-ядро APPROVE; UI dashboard оставлен НАМЕРЕННО нетронутым — проводка per-task строк / per-task Стоп / неблокирующего старта = M12.6 (status per-task) + M12.7/M12.8 (UI). До них дашборд ведёт себя как раньше (одна задача на аккаунт через UI), новая конкуренция доступна только через `/api/start` напрямую. +8 тестов (конкуренция 3 задач, stop по task/без task/alias/unknown→400, stale-exit identity). npm test 1106→1116 зелёных.]
- [x] (M12.6) Heartbeat/статус по ЗАДАЧЕ: `logs/status/<account>__<task>.json` (review.js/daemon-шаги пишут свой файл) — чтобы статусы трёх параллельных задач не перетирали друг друга; `liveStatus`/`collectLive` читают per-task. Тест чтения нескольких. — Acceptance: три статуса одного аккаунта не конфликтуют.
- [x] (M12.7) UI «Управление»: кнопки задач НЕ блокируют друг друга; статус, тумблер Live и Стоп — отдельно на КАЖДУЮ задачу (а не одна строка на аккаунт). — Acceptance: можно запустить и независимо останавливать каждую из трёх; статусы раздельные.
- [x] (M12.8) UI «Сейчас»: несколько активных задач на аккаунт (apply/messages/resume) — каждая своей строкой со своей фазой/прогрессом/живостью. — Acceptance: при параллельном запуске видно три независимых прогона.

---

## M13 — Скорость: человеческий пейсинг откликов 0.5–3.5 с + быстрый refresh панели ▶ DONE
Решение пользователя (2026-07-01, подтверждено уточнением «и то, и другое»): (1) пауза между
откликами должна быть **0.5–3.5 с** (сейчас дефолт 2–7 с, review.js:51-52) — быстрее и
человекоподобнее; (2) панель должна обновляться за <1 с (был polling 5 с). Бэкенд остаётся
Node.js — пользователь подтвердил: **FastAPI НЕ добавляем**, Electron (M15) оборачивает Node
напрямую. Инвариант: антибот-пейсинг только в боевом режиме (в `--dry-run` не тормозим), пауза
не после последней вакансии — сохранить (review.js:1840-1848).

- [x] (M13.1) Снизить `setInterval` в `loadLive` и `loadControl` с 5000 до 1000 мс (PAGE-шаблон dashboard.js). — Acceptance: видимый лаг обновления ≤ 1 с; `npm test` зелёный.
- [x] (M13.2) Пейсинг откликов 0.5–3.5 с: `DEFAULT_MIN_DELAY_SEC` 2→0.5, `DEFAULT_MAX_DELAY_SEC` 7→3.5 (review.js:51-52). Убедиться, что `randomDelayMs(minMs,maxMs)` и `parseArgs`-валидация (`--min-delay`/`--max-delay`, review.js:114-122) корректны на дробных секундах (0.5·1000=500 мс); обновить console-строку «Антибот: пауза …с» и любые примеры в README/CLAUDE.md/docs, где фигурирует 2–7. Тест `pacing.js` на дробных границах (min=0.5,max=3.5 → результат в [500,3500] мс). — Acceptance: боевой прогон делает паузу 0.5–3.5 с между откликами; `npm test` зелёный.
- [x] (M13.3) SSE-эндпоинт `GET /api/stream` в `dashboard.js`: сервер каждые 400 мс проверяет mtime файлов `logs/status/*.json` + `logs/resources.jsonl`; при изменении pushит `data: <json-снимок>\n\n`. Клиент: `new EventSource('/api/stream')` заменяет `setInterval loadLive`; fallback — polling 2 с если SSE недоступен. Слушать только `127.0.0.1` (как остальной сервер). Тест: mock-файл изменяется → событие отправлено. — Acceptance: задержка обновления < 500 мс от записи heartbeat.

## M14 — Метка «лимит откликов достигнут» ▶ DONE
hh.ru блокирует отклики при достижении дневного лимита — показывает баннер/текст на странице
отклика. Детект → `status='limit_reached'` + heartbeat `state='limit'` → graceful-стоп прогона
(дальше откликаться бессмысленно) → оранжевый badge «Лимит откликов» в панели «Сейчас».
ЧИСТЫЕ паттерны+state+badge можно сделать автономно (текстовые regex, как `APPLIED_PATTERNS`);
**живая проверка формулировок hh.ru — follow-up с пользователем** (паттерны легко дополнить, как
любые text-паттерны в `playwright.md`).

- [x] (M14.1) `src/lib/limit.js`: `LIMIT_PATTERNS` (русские формулировки дневного лимита hh.ru: «вы откликнулись на максимальное количество вакансий», «лимит откликов исчерпан», «достигли лимита откликов», «сегодня больше нельзя откликаться» — кириллица-safe regex БЕЗ `\b`/`\w`-границ на кириллице, урок M11.6) + `isLimitReached(pageText)` (guard на не-строку, never-throws). Пометить в JSDoc: формулировки нуждаются в живой верификации от пользователя. — Acceptance: `npm test` зелёный; модуль матчит сид-формулировки, не бросает на мусоре. ПОМЕТКА в PROGRESS: live-verify паттернов = follow-up.
- [x] (M14.2) Детект лимита в `reviewVacancy`: перед/после клика отклика проверить текст страницы через `isLimitReached`; при совпадении → результат `status='limit_reached'`, heartbeat `state='limit'`, цикл прерывается gracefully (`break`, не бросает; finally закрывает браузер). Untrusted текст страницы только `.test()`-матчится, наружу не эхо-ится (prompt-injection-safe). Тест на фикстуре/мок-результате. — Acceptance: `npm test` зелёный; при лимите прогон останавливается, heartbeat несёт `state='limit'`.
- [x] (M14.3) UI-badge: `accountLiveness` (liveStatus.js) распознаёт `state='limit'` → новый литерал `LIVENESS_LIMIT='limit'` (приоритет рядом с captcha); `formatPhase` (runPhase.js) → `phaseLabel='Лимит откликов'` при `state==='limit'`; dashboard.js рендерит оранжевый badge (класс `lv-limit`/`st-stop`-стиль). Тесты `formatPhase` + `accountLiveness` на `state='limit'`. — Acceptance: при `state='limit'` панель «Сейчас» показывает метку «Лимит откликов».

## M15 — Electron десктоп-приложение ▶ DONE
Обернуть существующий Node.js сервер в Electron: `BrowserWindow` открывает `http://127.0.0.1:8787`,
tray-иконка, автозапуск сервера. Бэкенд (`dashboard.js`, `daemon.js`, `review.js`) не меняется.
Новая зависимость `electron` — только devDependency, не меняет production-поведение CLI.
**Пользователь подтвердил (2026-07-01): десктоп = Electron поверх Node, FastAPI/Python НЕ
добавляем** — вся автоматизация (Playwright), DeepSeek-клиент и ~1123 теста остаются на Node.

- [x] (M15.1) `npm install --save-dev electron`; `src/electron-main.js`: запускает дочерний процесс `node src/dashboard.js`, ждёт порт, создаёт `BrowserWindow` 1280×800 с `http://127.0.0.1:8787`; при закрытии окна — завершает дочерний процесс. `package.json` script `"electron": "electron src/electron-main.js"`. `node --check src/electron-main.js` ок. — Acceptance: `npm run electron` открывает окно с панелью.
- [x] (M15.2) Tray-иконка (`assets/tray.png` 16×16 PNG, нарисовать программно через `nativeImage`): меню «Открыть панель», «Стоп всех задач» (POST /api/stop по каждому аккаунту), «Выйти»; закрытие окна → скрыть в трей, не завершать приложение. — Acceptance: иконка появляется, меню работает.
- [x] (M15.3) Автозапуск с системой (Windows): пункт меню трея «Запускать при старте» → `app.setLoginItemSettings({openAtLogin: true/false})`; состояние persists между запусками. — Acceptance: toggle работает, `app.getLoginItemSettings().openAtLogin` отражает выбор.

## M16 — LIVE-задачи доходят до конца: дочерние процессы как Node + без stdin-ловушек ▶ DONE
Боль (2026-07-01, от пользователя + прямое наблюдение в проде): LIVE-задачи, запущенные из
панели/демона, НЕ доходят до конца. Две подтверждённые причины:
  1. **Apply виснет на `collecting`.** Дочерние процессы спавнятся `spawn(process.execPath,…)`
     (daemon.js:84 `spawnNode`, taskRunner.js, electron-main.js). Когда цепочка стартует из
     Electron, `process.execPath` = `electron.exe` БЕЗ `ELECTRON_RUN_AS_NODE` → review.js/daemon.js
     исполняются как Electron-app, а не Node → Playwright/навигация виснут. Плюс сбор вакансий и
     открытие chatik-фрейма не имеют таймаут-гарда → любой стопор зависает навсегда.
  2. **Messages виснут на подтверждении.** `daemon.js:212-214`: при `!replyAuto` зовётся
     `confirm()` (prompts.js readline на stdin) — а интерактивного stdin нет (панель/демон/headless)
     → блокировка навсегда. Сгенерированный ответ так и не уходит.
**Инвариант безопасности:** НИ ОДНО реальное действие (отклик/отправка) не должно зависеть от
интерактивного stdin при запуске из панели/демона; dry-run по-прежнему ничего не шлёт; honesty-
гардрейлы и `decideSend`-гейт (replyAuto===true строго) НЕ ослаблять; `live` — только явный opt-in.
**security-reviewer обязателен** (исходящие действия на реальном аккаунте + spawn/env).

- [x] (M16.1) `src/lib/spawnEnv.js`: чистый `nodeSpawnEnv(baseEnv = process.env)` → копия env с `ELECTRON_RUN_AS_NODE: '1'` (никогда не мутирует вход; не тащит секреты в лог). Проводка во ВСЕ внутренние spawn `process.execPath`: `electron-main.js` (→ dashboard), `taskRunner.js` (→ daemon), `daemon.js` `spawnNode` (→ review). Под обычным `node` переменная игнорируется (безопасно/no-op). Тест чистой функции (ставит флаг, не мутирует baseEnv, сохраняет прочие ключи). — Acceptance: `npm test` зелёный; LIVE-apply из панели доходит до фазы `applying` (не виснет на `collecting`); `node --check` всех тронутых файлов ок.
- [x] (M16.2) TTY-aware подтверждения в `prompts.js`: чистый `resolveConfirmPolicy({ isTTY, autoFlag })` → `'auto' | 'prompt' | 'decline'` (нет TTY и нет autoFlag → `'decline'`, не зависание; есть autoFlag → `'auto'`; TTY → `'prompt'`). `confirm()`/`ask()` при `!process.stdin.isTTY` НЕ читают stdin (не виснут) — возвращают безопасный отказ (для confirm → `false`). Тест чистой политики (матрица isTTY×autoFlag). — Acceptance: запуск без TTY НИКОГДА не виснет на подтверждении; с TTY поведение прежнее; `npm test` зелёный.
- [x] (M16.3) Messages реально работают из панели: `daemonArgs` (`parseDaemonArgs`/`buildReviewChildArgs`-симметрия) + `taskControl.buildTaskCommand` прокидывают `--reply-auto` для `messages` ТОЛЬКО когда `live` (оператор уже подтвердил Live в панели + confirm()-диалог); dry-run → preview без отправки. `runMessagesPass`: `replyAuto` из флага; `confirmFn` подключается ТОЛЬКО при TTY (через M16.2). `decideSend`-гейт и honesty не трогаем (replyAuto===true строго, dry-run/not-confirmed не шлёт). Тесты `daemonArgs` (флаг только при live) + `decideSend` (dry-run/без флага → не шлёт). — Acceptance: LIVE messages из панели отправляют ответы автономно; dry-run и не-live ничего не шлют; без TTY не виснет; `npm test` зелёный.
- [x] (M16.4) Таймаут-гарды против вечного зависания: обернуть открытие chatik-фрейма (`getChatFrame`, messages.js) и навигацию/сбор в `collectVacanciesForAccount` (review.js) в таймаут (`Promise.race` с `AbortSignal.timeout`-style или существующий resilient-паттерн `.catch`); при превышении → heartbeat `state` ошибки + graceful-стоп шага (не бросать наружу, finally закрывает браузер). Чистый хелпер `withTimeout(promise, ms, onTimeoutValue)` + тест; проводка — resilient. — Acceptance: при недоступном фрейме/зависшей странице задача завершается за разумное время с ошибкой в heartbeat, НЕ виснет; `npm test` зелёный.
- [x] (M16.5) Безопасность подтверждений из GUI (документировать + follow-up): записан инстинкт `.claude/instincts/panel-daemon-spawned-no-stdin.md` — «panel/daemon-spawned LIVE действия не должны зависеть от терминального stdin; подтверждение оператора = Live-opt-in в панели». Заметка в PROGRESS: per-action подтверждение В САМОЙ панели (модалка перед каждым реальным откликом/отправкой) — отдельный будущий UX-апгрейд. — Acceptance: инстинкт записан; заметка в PROGRESS. [Ralph не смог записать в `.claude/**` (headless permission-гейт); оркестратор дописал инстинкт вручную.]

## M17 — Панель: live по умолчанию (без dry-run/тумблера) + подробные цифры и график текущего прогона ▶ DONE
Решение владельца аккаунтов (2026-07-01): из панели всё должно идти СРАЗУ в live — dry-run и
Live-тумблер убрать из UI, «Старт» = реальный прогон. Плюс показывать подробные цифры и график
ТЕКУЩЕГО прогона.
**Дизайн (безопасность сохранена):** бэкенд-дефолт остаётся safe (`buildTaskCommand` live=false,
`daemonArgs` dryRun=true) — нужно для CLI/тестов/`verify-live-flow`, не ослабляет защиту для
не-панельных вызовов. Меняем ТОЛЬКО панель: `startTask` всегда шлёт `live:true`, тумблер убран,
перед стартом ОДНО подтверждение «реальный прогон?» (единственный человеческий гейт вместо тумблера).
honesty-гардрейлы, `decideSend` (replyAuto при live), loopback-бинд — НЕ трогаем. `--dry-run`
остаётся CLI/тест-флагом (убираем только из панели и из UX, не из кода). **security-reviewer обязателен.**

### A. Live по умолчанию в панели
- [x] (M17.1) Убрать Live-чекбокс из блока «Управление» (dashboard.js PAGE/`loadControl`): `startTask(i, task)` всегда шлёт `{ task, account, live: true }`; перед POST `/api/start` — ОДИН `confirm('Запустить РЕАЛЬНЫЙ прогон «<task>» для «<account>»? Это реальные отклики/ответы на hh.ru.')`; убрать `id="live-<i>-<tk>"`-инпуты и чтение чекбокса; статус-строка задачи всегда «LIVE» (ветка «dry-run» из UI убрана). Обновить вводный текст блока «Управление» (убрать «Дефолт — dry-run; тумблер Live…»). Серверный live-инвариант (`body.live===true`, loopback-бинд, `isLoopbackRequest`) НЕ трогаем — панель просто всегда передаёт live. — Acceptance: в панели нет чекбокса Live; один «Старт» + подтверждение → live-задача; повторный старт той же пары по-прежнему 409; `npm test` зелёный; security-reviewer PASS.
- [x] (M17.2) Документация: в `CLAUDE.md` (раздел Non-negotiables/Commands) и `docs/daemon.md` зафиксировать: ПАНЕЛЬ запускает live по умолчанию (с обязательным confirm-диалогом), бэкенд-дефолт остаётся dry-run-safe для CLI/тестов, `--dry-run` — явный безопасный режим (verify-live-flow). honesty/confirm-гейт сохранены. — Acceptance: доки отражают новое поведение панели и не противоречат коду/инвариантам.

### B. Подробные цифры и график текущего прогона
- [x] (M17.3) Подробные счётчики ТЕКУЩЕГО прогона в панели «Сейчас». Источник — `buildRunCounters` (M12.3, уже считает): просмотрено / отправлено / пропущено / уже-откликнулись / manual / ошибки / лимит; токены (prompt / completion / cache-hit / всего), вызовы, оценка $. Проводка: review.js на каждом шаге кладёт компактный снимок счётчиков (только ЧИСЛА, без PII) в heartbeat (`buildHeartbeat`/statusWriter — новое опц. поле `counts`) или sidecar `logs/status/<acc>__<task>.counts.json`; `liveStatus`/`collectLive` поднимают его; панель рендерит плиткой/таблицей на аккаунт-задачу. Чистые части (passthrough `counts` в buildHeartbeat + liveStatus) — с тестами; без PII (тест). — Acceptance: при активном прогоне видны подробные цифры ТЕКУЩЕГО запуска, обнуляются на новый; `npm test` зелёный.
- [x] (M17.4) Мини-график текущего прогона (Chart.js — уже подключён, БЕЗ новых зависимостей): donut/bar разбивки исходов текущего прогона (отправлено / пропущено / уже-откликнулись / ошибки) + прогресс-бар фазы (`index/total` из heartbeat). Рендерится в блоке «Сейчас», обновляется по SSE/поллингу, сбрасывается на новый прогон. Графики «Истории» (агрегаты за всё время) не ломать. — Acceptance: при активном прогоне виден график текущей разбивки/прогресса; `node --check` ок; `npm test` зелёный.

## M18 — Панель: убрать confirm + починить падающие задачи (отклики-«таймаут», сообщения-мгновенный сбой) ▶ DONE
Боль (2026-07-01, от владельца аккаунтов, прямое наблюдение в панели): (1) задача **Отклики**
почти сразу показывает «Ошибка: таймаут» — срабатывает M16.4-гард (`collectVacanciesForAccount`
> `COLLECT_VACANCIES_TIMEOUT_MS=180000`, review.js:59,1775) и пишет error-хартбит с причиной-литералом
`timeout` (runPhase.js → «Ошибка: таймаут»). Общая причина `timeout` СКРЫВАЕТ настоящую (вероятно:
разлогин/редирект на вход, зависшая навигация поиска, либо пустой поиск). (2) задача **Сообщения**
«падает почти сразу после запуска» — дочерний `daemon.js --task messages` → `runMessagesPass` →
`processUnread` быстро выходит/падает (getChatFrame=null после 20с, сбой навигации `CHAT_URL`, либо
брошенное исключение), и в панели задача мгновенно исчезает/краснеет. (3) владелец просит **убрать
confirm** из панели — «Старт» должен запускать сразу, без диалога (M17.1-гейт убрать).
**Инвариант (безопасность M11–M17 сохраняется):** сервер слушает только `127.0.0.1` + loopback-guard;
live-запуск — строго `body.live===true`; honesty-гардрейлы и `decideSend`-гейт НЕ ослаблять;
бэкенд-дефолт остаётся dry-run-safe (`buildTaskCommand` live=false, `daemonArgs` dryRun=true) для
CLI/тестов/`verify-live-flow` — меняем ТОЛЬКО UI-панель и наблюдаемость. Untrusted-текст страницы
только `.test()`-матчится, наружу/в лог не эхо-ится (prompt-injection-safe). Чистые части — модуль +
тест; **security-reviewer обязателен** на M18.1 (убираем человеческий гейт перед live) и на любые
изменения детекта/сети. DOM-задачи (живые формулировки hh.ru) — research-first: acceptance = чистый
парсер на сохранённом санитизированном фикстуре + пометка о ручной проверке.

### A. Убрать confirm из панели
- [x] (M18.1) Убрать `confirm()` из `startTask` (dashboard.js PAGE): «Старт» сразу шлёт `{task, account, live:true}` без диалога; обновить вводный текст блока «Управление» (убрать «перед запуском — одно подтверждение»). Server live-инвариант (`body.live===true`, loopback-bind, `isLoopbackRequest`) НЕ трогать. Обновить `CLAUDE.md` (Non-negotiables/Commands) и `docs/daemon.md`: панель запускает live БЕЗ confirm-диалога (по решению владельца аккаунтов 2026-07-01), бэкенд-дефолт остаётся dry-run-safe, honesty/`decideSend`/loopback сохранены. — Acceptance: в панели нет confirm; «Старт» → live-задача; повторный старт той же пары по-прежнему 409; `npm test` зелёный; security-reviewer PASS (подтвердить: гейт убран ОСОЗНАННО владельцем, прочие защиты на месте).

### B. Отклики — снять ложный «таймаут», вскрыть настоящую причину
- [x] (M18.2) `src/lib/collectState.js`: чистый `detectCollectProblem(pageTextOrSignals)` → `'logged_out'` (разлогин/редирект на вход: «Войти», «Войдите в аккаунт», URL `/account/login`), `'empty_search'` (страница поиска без вакансий: «Ничего не найдено», «По вашему запросу ничего не найдено»), `'ok'` (приоритет logged_out>empty>ok; guard на не-строку; кириллица-safe regex БЕЗ `\b`/`\w`-границ, урок M11.6; never-throws). JSDoc-пометка: формулировки нуждаются в живой верификации. — Acceptance: `npm test` зелёный; модуль матчит сид-формулировки, не бросает на мусоре.
- [x] (M18.3) Проводка причины в хартбит сбора (review.js `processAccount`): перед/при таймаут-ветке прочитать текст страницы (resilient `.innerText().catch('')`) → `detectCollectProblem`; если `logged_out`/`empty_search` → heartbeat `lastEvent` = соответствующий литерал (новые `ERROR_REASONS`/state, напр. `auth`/`empty`) вместо общего `timeout`; `formatPhase` (runPhase.js) → человекочитаемо «Нужен вход в аккаунт» / «Поиск пуст». Untrusted текст только `.test()`-матчится, наружу не эхо-ится. Тест `formatPhase` на новых причинах. — Acceptance: при разлогине/пустом поиске панель показывает понятную причину, НЕ «таймаут»; `npm test` зелёный.
- [x] (M18.4) Гранулярные таймауты ВНУТРИ сбора (review.js `collectVacanciesForAccount`/`collectFromSearch`): навигацию поиска и каждую страницу пагинации обернуть собственным таймаут-гардом (`withTimeout`/resilient `.catch`) с разумным под-лимитом, чтобы одна зависшая страница не съедала весь 180с-бюджет и частичный пул всё равно возвращался. Чистую часть (если выделяется) покрыть тестом; проводка — resilient. — Acceptance: при зависании одной страницы сбор завершается за разумное время с частичным пулом, не «таймаутит» весь шаг; `npm test` зелёный.

### C. Сообщения — не «падать» молча
- [x] (M18.5) Понятный исход поллинга вместо «падения»: в `runMessagesPass`/`processUnread` различать (a) штатный пустой результат (getChatFrame=null → «Чат не найден», 0 непрочитанных → «Нет новых сообщений») → heartbeat `phase='done'` с понятной меткой; (b) реальную ошибку → heartbeat `phase='error'` с причиной из `classifyErrorReason` (без PII/текста письма). Чистый формат-хелпер метки исхода + тест; `formatPhase` показывает их словами. — Acceptance: при пустом чате панель показывает «Чат не найден»/«Нет новых сообщений» (не выглядит сбоем); при реальной ошибке — её причина; `npm test` зелёный.
- [x] (M18.6) research-first (нужен живой лог/HTML от пользователя): зафиксировать НАСТОЯЩУЮ причину мгновенного сбоя сообщений — exit-код дочернего `daemon.js --task messages`, текст ошибки, состояние `CHAT_URL`/фрейма. Записать evidence в PROGRESS + при необходимости дополнить паттерны/таймауты. Если для починки нужен живой DOM — оформить как фикстуру + чистый парсер (acceptance-конвенция DOM-задач). — Acceptance: причина задокументирована в PROGRESS; если применимо — фикстура + тест парсера; ручная проверка помечена.

## M19 — Панель: индикатор разлогина + запуск логина из приложения ▶ DONE
Боль (2026-07-01, подтверждено живым прогоном): аккаунт **belonogov** завис на сборе выдачи
(session flagged/разлогинен/капча) — в панели это НЕ видно (просто нет прогресса / общий «таймаут»),
а перелогиниться можно только из терминала (`npm run login -- --account belonogov`). Нужно: (A) панель
ЯВНО показывает «Сессия разлогинена — нужен вход» по аккаунту; (B) кнопка **«Войти»** в панели
запускает headful-логин и по завершении СОХРАНЯЕТ сессию — не выходя из приложения.
**Инвариант (безопасность M11–M18 сохраняется, УСИЛЕН):** сервер слушает только `127.0.0.1` +
`isLoopbackRequest`-guard; логин открывает headful-браузер и СОХРАНЯЕТ `storage-state.json` =
**credentials** → cookies/PII НЕ логировать и НЕ отдавать в UI; сессия строго скоуплена по аккаунту
(`getAccountStorageStatePath`, без утечки между аккаунтами); login-задача трекается/останавливается как
остальные (одна на аккаунт → дубль 409); honesty/live/`decideSend`-инварианты не трогаем. Логин НЕ шлёт
откликов, поэтому `--live` ему не нужен, но headful-spawn + запись сессии = **обязателен
security-reviewer**. DOM-детекторы (залогинен/разлогинен) — research-first: живой HTML от пользователя,
acceptance = чистый детектор на фикстуре + пометка ручной проверки.

### A. Видно, что сессия разлогинена
- [x] (M19.1) `liveStatus.accountLiveness` распознаёт `heartbeat.state === 'logged_out'` → новый литерал `LIVENESS_LOGGED_OUT='logged_out'` (приоритет рядом с captcha/limit, ВЫШЕ stalled — разлогин корневая причина); `formatPhase` (runPhase.js) при `state==='logged_out'` → `phaseLabel='Сессия разлогинена — нужен вход'`. Чистые части + тесты (`accountLiveness`/`formatPhase` на `state='logged_out'`). — Acceptance: `npm test` зелёный; при `state='logged_out'` liveness и phaseLabel корректны. [СДЕЛАНО (2026-07-01): `LIVENESS_LOGGED_OUT='logged_out'` (liveStatus.js), приоритет captcha>limit>logged_out>stalled/idle (early-return до done→idle); `formatPhase` → «Сессия разлогинена — нужен вход» (captcha>limit>logged_out>фаза). Литерал совпадает с `COLLECT_LOGGED_OUT` (collectState.js) — без drift. +8 тестов (приоритеты, done-vs-logged_out). code-reviewer APPROVE (0 блок., нит закрыт). npm test 1296 зелёных.]
- [x] (M19.2) Проводка детекта в живой флоу: в `collectVacanciesForAccount`/на таймаут-ветке сбора (review.js) и в messages-пути читать текст+URL страницы (resilient `.catch('')`) → `detectCollectProblem` (collectState.js, M18.2, уже есть); при `logged_out` → heartbeat `state='logged_out'` вместо общего timeout/error. ВАЖНО: когда страница ЗАВИСАЕТ на `goto` (кейс belonogov — текст не прочитать), после per-page-таймаута сделать быструю проверку `page.url()`/лёгкий `innerText` под коротким гардом — если редирект на login или пусто, всё равно попытаться классифицировать (best-effort, не виснуть). Untrusted текст только `.test()`. Тест чистого маппера. — Acceptance: при разлогине панель показывает «Сессия разлогинена», не «таймаут»; `npm test` зелёный. [СДЕЛАНО (2026-07-01): чистый маппер `collectProblemHeartbeat(problem)` (collectState.js) → `{lastEvent,state}`: logged_out→{auth,'logged_out'}, empty→{empty,'ok'}, иначе {timeout,'ok'}; литералы причин из единого `ERROR_REASONS` (без дрейфа), `HEARTBEAT_STATE_LOGGED_OUT===COLLECT_LOGGED_OUT`. Проводка: review.js таймаут-ветка сбора (уже best-effort читала body innerText/timeout 3000 + page.url() с M18.3) теперь ставит `state` из маппера → при разлогине `state='logged_out'`. messages-путь: `processUnread` `!frame`-ветка best-effort читает текст+URL → возвращает `loggedOut`; daemon `runMessagesPass` при `loggedOut` пишет heartbeat phase=error+state='logged_out' (иначе прежний DONE «Чат не найден»). Untrusted текст только `.test()`-матчится, наружу/в лог не эхо-ится (console.log печатает только фикс-литералы). +6 тестов маппера. code-reviewer APPROVE (0 блок./should-fix, 2 опц. нита оставлены осознанно). npm test 1302 зелёных. UI-badge (красная точка `lv-logged-out` + призыв «→ Войти») — M19.3; security-reviewer login-эндпоинтов — M19.7.]
- [x] (M19.3) dashboard UI: в блоке «Сейчас» для `liveness==='logged_out'` — красная точка (класс `lv-logged-out`) + метка «Сессия разлогинена» + инлайн-призыв «→ Войти» (скроллит/подсвечивает кнопку из M19.6). Существующие индикаторы не ломать. — Acceptance: при `state='logged_out'` виден явный badge с призывом войти. [СДЕЛАНО (2026-07-01): CSS `.lv-logged-out` (красная точка) + легенда блока «Сейчас»; `LIVENESS_LABEL.logged_out='разлогин'` (короткий чип рядом с завис/капча); метка «Сессия разлогинена — нужен вход» приходит из `phaseLabel`/`formatPhase` (M19.1). Инлайн `→ Войти` (только при liveness==='logged_out') → `scrollToLogin(account)` скроллит+подсвечивает per-account контейнер «Управление» (якорь `accCtlId`, единый нормализатор для set/resolve id; fallback `#controlBody`, всегда резолвится) — кнопка входа появится в M19.6, деградирует мягко. Изменена общая строка рендера только `+ loginCall` (='' для всех прочих state → остальные индикаторы байт-в-байт). account эскейпится через esc() в data-атрибут, id инъекция-safe. code-reviewer APPROVE WITH NITS (0 блок./should-fix; нит = косметика чипа). node --check ок. npm test 1302 зелёных.]

### B. Запуск логина из панели
- [x] (M19.4) **Не-stdin завершение логина** (ключевая проблема: `login.js:38` ждёт `ask('нажмите Enter')` — из панели без TTY M16.2 вернёт отказ сразу → сохранит НЕзалогиненную сессию). Решение (research-first, выбрать в ходе): (вариант по умолчанию, без угадывания селекторов) `src/login.js --panel` открывает headful-браузер на hh.ru/login и ждёт СИГНАЛ ИЗ ПАНЕЛИ, а не stdin — поллит sentinel-файл `logs/login-<account>.done` (пишется эндпоинтом M19.5); по сигналу → `context.storageState({path})` + закрыть; по таймауту (напр. 5 мин) → выход БЕЗ перезаписи (не портим существующую сессию). Пишет heartbeat (`task='login'`, phase `login_waiting`/`login_saved`/`login_timeout`). (альтернатива, если пользователь даст живой HTML залогиненного hh.ru) авто-детект `isLoggedIn(pageSignals)` (URL не /login + logged-in DOM-сигнал) → авто-сохранение. Чистая часть (poll-хелпер или `isLoggedIn`) + тест; sentinel-путь в `.gitignore`. — Acceptance: логин из панели НЕ виснет на stdin и НЕ сохраняет пустую сессию; при завершении входа сессия сохраняется; `npm test` зелёный. **security-reviewer**. [СДЕЛАНО (2026-07-01): чистый `src/lib/loginSignal.js` — `waitForLoginSignal({isDone,isStopped,now,sleep,timeoutMs,pollMs})` → `{outcome,waitedMs}` (приоритет done>stopped>timeout, never-throws, нормализация мусорных опций; LOGIN_PHASES/LOGIN_OUTCOMES заморожены; дефолт 5 мин/1 с) + 10 тестов. `getLoginSentinelPath(account)`→`logs/login-<acc>.done` (config.js, через normalizeAccountName → traversal-safe), git-ignored. `login.js --panel`: НЕ читает stdin (нет confirm/ask), снимает устаревший sentinel, headful goto hh.ru/login, heartbeat login_waiting, `waitForLoginSignal` поллит existsSync(sentinel); `storageState({path})` ТОЛЬКО при 'saved' + heartbeat login_saved; иначе — БЕЗ записи + login_timeout (таймаут не портит существующую сессию); finally закрывает браузер + прибирает sentinel. Не-panel ветка (интерактив ask) байт-в-байт прежняя. isStopped в панели пока не проброшен (комментарий-заметка для M19.5). code-reviewer APPROVE WITH NITS (нит #1 — комментарий про будущую ветку STOPPED — внесён). security-reviewer PASS (0 crit/high; 2 LOW = follow-up M19.5: проброс isStopped для «Стоп», loopback/CSRF-гард эндпоинта /api/login-done). node --check 3 файла ок. npm test 1302→1312 зелёных.]
- [x] (M19.5) `taskControl`/`taskRunner` поддерживают тип `login`: спавнят `node src/login.js --panel --account <name>` (headful; ОТДЕЛЬНЫЙ script-путь — НЕ daemon.js; taskRunner выбирает скрипт по типу задачи). `login` в `ALLOWED_TASKS`/`normalizeTask`; `canStart` pair-aware (login — свой тип, дубль login на аккаунт → 409). `--live` для login НЕ применяется (сохранение сессии ≠ отправка). Новый эндпоинт `POST /api/login-done {account}` (loopback-guard как /api/start) → пишет sentinel `logs/login-<account>.done`, который читает M19.4. Тесты argv/canStart/эндпоинта. — Acceptance: login-задача стартует/трекается/стопается; дубль → 409; сигнал завершения доходит; bind только loopback; `npm test` зелёный. **security-reviewer**. [СДЕЛАНО (2026-07-01): `login` в `ALLOWED_TASKS` (taskControl.js, без алиасов); `buildTaskCommand({task:'login'})` возвращает login-argv `['--panel','--account',name]` (НЕ daemon-argv, ранний return до daemon-блока) — без `--live`/limit/text/area. taskRunner: `LOGIN_JS`-константа + инъекция `loginPath`; `start` теперь берёт task/account из нормализованных `opts` (не позиционно из argv — login-argv не daemon-shaped), `live = task!=='login' && opts.live===true` (login принудительно non-live даже при `live:true`), `scriptPath = login→login.js иначе daemon.js`, аудит-префикс «Логин запущен оператором». Рефактор argv→opts байт-идентичен для apply/messages/resume/алиасов (argv[1] и так был normalizeTask(opts.task)). dashboard: `/api/login-done` в isControl-сете (loopback+Origin-гард как /api/start) → POST-хендлер (body-parse→account-валидация(пусто→400)→writeLoginDone→200 `{ok,account}`, throw→500), инъекция `deps.writeLoginDone` (дефолт `writeFile(getLoginSentinelPath(account),'')` — пустой sentinel, existence-only). code-reviewer APPROVE (0 should-fix, 2 benign нита); security-reviewer PASS (0 crit/high/med: loopback+CSRF-гард, только имя аккаунта наружу/в лог, normalizeAccountName проверено на traversal-payload'ах, empty-sentinel без инъекции, spawn argv-массивом без shell, force-non-live, `logs/login-*.done` git-ignored). +16 тестов (6 taskControl, 6 taskRunner, 4 dashboard). node --check 3 файла ок. npm test 1312→1328 зелёных.]
- [x] (M19.6) dashboard «Управление»: per-account кнопка **«Войти»** (рядом с Отклики/Сообщения/Резюме) → `POST /api/start {task:'login', account}`; пока идёт — кнопка «Готово (сохранить вход)» → `POST /api/login-done {account}` + «Стоп». Статус login (идёт/сохранено/таймаут) отдельной строкой. При `liveness==='logged_out'` кнопка «Войти» подсвечена. — Acceptance: клик открывает headful-логин; после входа «Готово» сохраняет сессию; панель отражает статус; повторный старт login того же аккаунта отклонён. [СДЕЛАНО (2026-07-01): чисто клиентская правка PAGE-шаблона dashboard.js. Отдельная строка «Вход» в per-account блоке «Управление» (IIFE зеркалит task-row паттерн): кнопки «Войти»/«Готово (сохранить вход)»/«Стоп». «Войти» → `startTask(i,'login')` (POST /api/start {task:'login',live:true} — сервер force-non-live'ит login, M19.5, live/text/area/limit игнорит); «Готово» → новый `loginDone(i)` (POST /api/login-done {account}, endpoint из M19.5, пишет пустой sentinel); «Стоп» → stopTask(i,'login'). Статус login-строки из `byAccTask[acc+'\0'+'login']` (taskRunner.list task:'login'): идёт вход·pid / остановлено / простаивает; disable-логика как у task-row. `loggedOutAccounts` пересобирается в renderLive из liveness==='logged_out' → подсветка «Войти» классом `.ctl-login-need` (красная рамка). data-action="login"/"login-done" — отдельные значения, не коллидят со start/stop; account не эхо-ится в HTML (только data-idx=integer, esc(run.pid)) → XSS-surface не добавлен. scrollToLogin-комментарий (M19.3) обновлён. code-reviewer APPROVE WITH NITS (0 blocking/should-fix; ниты: dead `.ctl-login` base-класс + отсутствие in-flight guard на loginDone — оба совпадают с house-style startTask/stopTask, оставлены). node --check ок. npm test 1328/1328 зелёных. Финальный security-reviewer login-эндпоинтов/headful/credentials = M19.7.]
- [x] (M19.7) security-reviewer финальный: сохранение `storage-state.json` (credentials) + headful-spawn + новый эндпоинт `/api/login-done`. Проверить: bind только `127.0.0.1` + `isLoopbackRequest` на login-эндпоинтах; cookies/PII/содержимое сессии не логируются и не уходят в UI (только статус/имя аккаунта); сессия скоуплена по аккаунту; sentinel git-ignored; `--live`/honesty/`decideSend` не тронуты. — Acceptance: security-reviewer PASS (0 crit/high). [СДЕЛАНО (2026-07-01): security-reviewer PASS (0 crit/0 high) по диффу `223a38a..HEAD`. Все 6 осей чисты: (1) credentials/PII — `storageState({path})` пишет cookies СТРАЗУ на диск (login.js:61,91), в переменную/лог/HTTP не попадают; console печатает только путь+имя аккаунта; `/api/login-done` отдаёт только `{ok,account}`; taskRunner логирует только task/account/live/pid. (2) скоупинг/traversal — sentinel и storage-path оба через `normalizeAccountName` (config.js:17-22, `[^\w.-]`→`-`, пусто→default), `../`/абсолют/NUL/сепараторы нейтрализуются; producer(dashboard)/consumer(login.js) нормализуют идентично → без кросс-аккаунт-утечки. (3) сеть/CSRF — `/api/login-done` в `isControl`-сете + `isLoopbackRequest` (Host+Origin, unparseable Origin→reject) как `/api/start`, НЕ в read-only bypass; bind 127.0.0.1; body cap 64 KiB. (4) spawn — argv-массив без shell (taskRunner.js:104), `--live` для login структурно невозможен (buildTaskCommand фикс-argv taskControl.js:69-71 + `live=task!=='login'&&opts.live===true` taskRunner.js:92). (5) sentinel — content-free `writeFile(path,'')`, existence-only `existsSync`, устаревший снимается ДО старта браузера (login.js:40) → пустая сессия не авто-сохранится; +finally cleanup. (6) инварианты — таймаут НЕ зовёт storageState (login.js:64-70) → рабочая сессия цела; decideSend/honesty/live-opt-in не тронуты; `waitForLoginSignal` never-throws. `.gitignore:18` `logs/login-*.done` (git check-ignore подтверждён). 2 инфо/low (isStopped не проброшен → «Стоп» как timeout, безвреден; writeLoginDone-catch глотает ошибку — предпочтительно, не течёт путь). node --check 6 файлов ок. npm test 1328 зелёных. Независимая перечитка оркестратора совпала с вердиктом. Ревью-only задача — код не менялся.]

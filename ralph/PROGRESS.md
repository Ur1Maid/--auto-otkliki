# PROGRESS — журнал Ralph (append-only)

Каждая итерация дописывает сюда одну запись. Формат:
`YYYY-MM-DD HH:MM | <ID> | DONE|BLOCKED | что сделано / прогнал / результат`

---

2026-06-24 | M0.1 | DONE | bootstrap: `npm test` + `test/smoke.test.mjs` (node --check на всех src/*.js).
2026-06-24 | M0.2 | DONE | bootstrap: ralph/ (PROMPT, ROADMAP, ralph.ps1, ralph.sh, README), ветка ralph/auto.
2026-06-25 | M1.1 | DONE | src/lib/text.js (parseJsonObject, cleanGeneratedAnswer) + test/text.test.mjs (12). implementer(Sonnet)→code-reviewer(APPROVE)→npm test 19/19. looksLikeEmployerVoice оставлен (M1.5).
2026-06-25 | M1.2 | DONE | src/lib/fields.js (detectFieldKind, isSalaryContext, isGenericFieldContext, getMainQuestion) + test/fields.test.mjs (22). implementer(Sonnet)→code-reviewer(APPROVE w/nits, blank-line fixed)→npm test 41/41.
2026-06-25 | M1.3 | DONE | src/lib/knowledge.js (RESUME_KEYWORDS[58], normalizeText, getSearchTerms, extractResumeKeywords, pickKnowledgeChunks) + test/knowledge.test.mjs (15). implementer→code-reviewer(APPROVE)→npm test 56/56.
2026-06-25 | M1.4 | DONE | src/lib/urls.js (normalizeHhUrl, normalizeVacancyUrl) + test/urls.test.mjs (6). implementer→self-review оркестратора (тривиальные URL-функции, byte-identical)→npm test 62/62.
2026-06-25 | M1.5 | DONE | src/lib/answers.js (matchesAnyPattern, looksLikeEmployerVoice, optionMatches; импортит normalizeText из knowledge) + test/answers.test.mjs (14). implementer→code-reviewer(APPROVE w/nits): honesty-регэксп employer-voice байт-в-байт; убран осиротевший импорт normalizeText. npm test 76/76. M1 ЗАВЕРШЁН.
2026-06-25 | M2.1 | DONE | src/lib/deepseek.js (callDeepSeek, byte-identical) + test/deepseek.test.mjs (7, fetch замокан). implementer→security-reviewer(PASS): ключ только в Authorization-заголовке, не логируется/не возвращается; сеть/timeout/деградация сохранены. npm test 83/83.
2026-06-25 | M2.2 | DONE | callDeepSeek: ретрай с backoff (500/1000мс) на transient (0/429/5xx), НЕ на 402/4xx; maxRetries=2, инъекция sleep для тестов. +5 тестов (счётчики вызовов, последовательность задержек). Старый network-тест переведён на no-op sleep. implementer→code-reviewer(APPROVE w/nits). npm test 88/88 за 240мс.
2026-06-25 | M2.3 | DONE | redactSecrets (по имени ключа, 1 уровень рекурсии) + appendDeepSeekDebug пропускает записи через него; +4 теста. implementer→security-reviewer(PASS, документ. ограничение: не ловит секрет в ЗНАЧЕНИИ — инстинкт debug-redaction-is-key-name-only). npm test 92/92. M2 ЗАВЕРШЁН.
2026-06-25 | M3.9/M3.10 (модули) | DONE | src/lib/vacancyExtract.js (extractRequirements) + src/lib/resumeProfile.js (buildResumeProfile) + тесты (15). Чистые, review.js не тронут. code-reviewer APPROVE + заметки для проводки (substring-маркеры, профиль — только в локальный скоринг). npm test 107/107. Проводка отдельным шагом.
2026-06-25 | M3.9 (проводка) | DONE | scoreVacancyWithDeepSeek шлёт extractRequirements(vacancyText) (<=1500) вместо ~9000 симв.; маркеры закрыты lookbehind от substring-срабатываний. Форма/сбор сигналов получают полный текст без изменений. +2 теста. self-review оркестратора (scope: 1 строка, gate/honesty не тронуты). npm test 109/109. Замечание: A/B против живого API недоступен — порог min-score остаётся страховкой.
2026-06-25 | M3.2/M3.10 | DONE | src/lib/localScore.js (localRelevanceScore, needsModelScoring) + проводка local-first в reviewVacancy: уверенный локальный reject пропускает DeepSeek (0 токенов). БЕЗОПАСНАЯ политика: локальный скоринг только ОТКЛОНЯЕТ, никогда не пропускает к отклику без модели; low привязан к minScore. resumeProfile (M3.10) — основа для локальной логики. +10 тестов. code-reviewer APPROVE w/nits (инвариант подтверждён, нит low<->minScore закрыт). npm test 119/119.
2026-06-25 | M3.13 | DONE | src/lib/coverLetter.js (coverLetterRequired, shouldGenerateCoverLetter) + проводка в openCoverLetterEditor (инлайн-regex → чистый предикат, детект обязательности байт-в-байт). Опциональные письма = 0 токенов (поведение детекта неизменно). shouldGenerateCoverLetter экспортирован+покрыт, в флоу не проведён (поведенческая привязка к баллу — отдельно). +14 тестов. self-review оркестратора. npm test 133/133.
2026-06-26 | M5.1+M5.4 | DONE | research-first по ЖИВОМУ HTML резюме hh.ru (от пользователя, D:/workplace/hh_resume_body.txt). EVIDENCE: нативная кнопка data-qa="resume-update-button"; активный лейбл «Поднять в поиске» (i18n applicant.resume.update.button.text); КУЛДАУН — лейбл меняется на «Обновить можно через {время}» (i18n myresume.publish.nextTouch); состояние резюме в встроенном JSON: canTouch:true/false, isSearchable, id, hash. selectors.js +RESUME_SELECTORS.updateButton +RESUME_BUMP_READY_PATTERNS [/поднять в поиске/i,/обновить дату/i] +RESUME_BUMP_COOLDOWN_PATTERNS [/обновить можно через/i]. src/lib/resumeBump.js: classifyBumpButton({visible,enabled,text})→{canBump,reason} (порядок not_found>cooldown>ready>disabled>unknown, консервативно) + bumpResume(frame,{dryRun}) тонкая resilient (dryRun ДЕФОЛТ true — клик только при canBump&&dryRun===false; кулдаун/not_found гасят до клика; не бросает). +24 теста (dry-run не кликает, cooldown/not_found гасят). M5.4: daemon.runMicroEditPass заменён с заглушки на реальный шаг — навигация /applicant/resumes + bumpResume (dryRun из opts), изоляция аккаунта, browser.close finally, report.recordResumeEdit(applied=bumped). Кулдаун hh.ru определяется самим classifyBumpButton → вызов каждые 30 мин дёшево (в кулдауне no-op). Мультирезюме → strict-mode → безопасный not_found (follow-up). ВАЖНОЕ ОТКРЫТИЕ подтвердилось: активность даёт кнопка на стороне hh.ru, НЕ локальный resume.md. npm test 815/815.
2026-06-26 | M7.4+M7.5 | DONE | M7.4: scripts/register-task.ps1 (Register-ScheduledTask: ежедневно node src/daemon.js, абсолютный путь node, рабочая папка=репо, StartWhenAvailable, лимит 12ч; idempotent — перерегистрирует; dry-run по умолчанию, live только через -DaemonArgs '--no-dry-run') + scripts/unregister-task.ps1. Документируют МСК/таймзону (Task Scheduler=локальное время, окно 09:00-18:00 МСК обеспечивает сам демон). ВАЖНО: пересохранены как UTF-8 BOM — иначе PowerShell 5.1 читает UTF-8 без BOM как cp1252 → кириллица в мусор → parse errors при реальном запуске. Оба parse-OK через Parser::ParseFile. npm-скрипт "daemon" добавлен в package.json. Авто-запуск не дёргали (acceptance). M7.5: docs/daemon.md (что наружу+гарды-таблица, флаги, как остановить DAEMON_STOP+SIGINT, автозапуск, наблюдаемость, безопасность/данные, известные ограничения: ключ из env, resumeProfile/salary не резолвятся, M5.1-блок). DAEMON_STOP в .gitignore. npm test 791/791.
2026-06-26 | M7.2-демон | DONE | src/daemon.js (капстоун-оркестратор, glue — собирает все чистые блоки; node --check ок, юнит-тестами не покрыт т.к. браузер/процессы) + src/lib/daemonArgs.js (чистые parseDaemonArgs/buildReviewChildArgs, тестируемо). Демон: главный цикл по decideNextAction (daemonPlan) → APPLY_RUN (spawn node src/review.js с buildReviewChildArgs) / POLL_MESSAGES (launchBrowser+processUnread по аккаунтам, изоляция аккаунта, browser.close в finally) / MICRO_EDIT (честная заглушка — требует M5.1 нативной кнопки, локальная правка resume.md неэффективна для hh.ru-активности) / IDLE (сон кусками по 10с с проверкой стопа) / STOP (вне рабочих часов МСК). Изоляция шагов runIsolatedTask, STOP-файл DAEMON_STOP + SIGINT, дневной отчёт dailyReport→logs/daily-*.json. SAFETY: dryRun ДЕФОЛТ true (громкое live-предупреждение при --no-dry-run/--live), buildReviewChildArgs в dry-run даёт --dry-run --manual без --yes, ключ/PII не логируются, --once для проверки. +42 теста daemonArgs (dryRun дефолт true, dry-run→--dry-run --manual без --yes). npm test 791/791. ЗАМЕТКА: messages-путь читает DEEPSEEK_API_KEY из process.env (демон сам .env не парсит — review-child парсит); без ключа generateReply деградирует до manual (безопасно). resumeProfile/salary в messages-контекст пока не резолвятся (документировать в M7.5).
2026-06-26 | M6.3 | DONE | src/lib/resumeWriter.js (safe IO-обёртка записи реального resume.md, fs инъектируется): writeTailoredResume({resumePath,approvedSkills,limits,dryRun,date}) — dryRun ДЕФОЛТ true (dryRun!==false для записи, coercion-safe), tailorResume (additive+дивергенция), applied!==true→no write, tailored===original→no_changes, dry-run→preview без записи; реальная запись: бэкап ДО перезаписи (backup_failed→resume НЕ трогаем), затем resume, write_failed→возвращает backupPath. Без логов/PII (preview только в dry-run). security-reviewer PASS-w/nits → закрыт LOW (нормализация невалидного Date перед backupFileName, контракт «не бросать»). +16 тестов (dry-run не пишет, бэкап-до-перезаписи порядок, backup_failed не перезаписывает, honesty addedSkills⊆approvedSkills). ПРИВЯЗКА: finishResumeUpgradeReport (review.js) теперь добавляет в отчёт машинно-применимые предложения (buildResumeSuggestions/M6.1) — отчёт-only, resume.md НЕ правится автоматически; реальная запись operator-gated (явные approvedSkills+dryRun:false), honesty запрещает авто-добавление неодобренного. npm test 748/748.
2026-06-26 | M4.7 | DONE | (A) src/lib/pollLoop.js — тестируемое ядро поллинг-цикла: runPollingLoop({iteration,shouldStop,intervalMs,sleep,maxIterations}) с инъекцией sleep/stop; shouldStop проверяется ДО итерации и перед сном (не спим после стопа), итерация в runIsolatedTask (сбой не роняет цикл), shouldStop-бросок=не-стоп, intervalMs нормализуется, maxIterations-предохранитель; не бросает наружу. +10 тестов (точный счёт sleep=iter-1, изоляция, стоп). (B) src/messages.js processUnread полностью реализован (resilient browser-glue, БЕЗ юнит-тестов — нужен браузер): getChatFrame→listThreads→фильтр unread→runIsolated по тредам (открыть ячейку, readThread, decideReply→skip/manual/needs_model→generateReply→sendReply). dryRun ДЕФОЛТ true, replyAuto false. code-reviewer APPROVE w/nits → ЗАКРЫТЫ: S1 (единообразный processed=осмотренные треды), S2 (manual/no-answer треды трекаются в сессии — не пере-открывать и не тратить токены повторно; reply-handshake add только после sent), N1 (мёртвая ветка replyAuto), N2 (guard битых элементов в lastEmployerMessage + тест), N3 (точный комментарий). PII письма не логируется; превью оператору = свой ответ. npm test 732/732.
2026-06-26 | M4.6 | DONE | (execution-слой, выбор пользователя «писать за safe-гардами») src/lib/replySend.js: createProcessedTracker (in-memory Set, String(chatId) → числовой/строковый id эквивалентны; идемпотентность за сессию, БЕЗ файла состояния); decideSend({dryRun,replyAuto,confirmed,alreadyProcessed}) ЧИСТЫЙ гейт fail-closed (приоритет already_processed>dry_run>not_confirmed; send только при replyAuto===true ИЛИ confirmed===true, строгое !==true); sendReply(frame,text,opts) тонкая resilient-отправка — текст валидируется ДО гейта, гейт ДО любого DOM (в dry-run/not_confirmed/already_processed НИ fill НИ click), fill→isEnabled→click в try/catch, наружу не бросает, лог без PII. Дефолт = подтверждение (--reply-manual). +30 тестов (мок frame, критичные safety: dry-run/not_confirmed не трогают DOM). security-reviewer PASS: гейт неообходим/неообходимо-fail-closed, нет утечек secret/PII (единственный лог — статичная строка), untrusted text только в fill(), не бросает. Ноты для M4.7: вызывающий обязан tracker.add(chatId) после отправки + передавать alreadyProcessed. npm test 721/721.
2026-06-26 | ГРАНИЦА | NOTE | Все ЧИСТЫЕ, не-выходящие-наружу, не-правящие-реальный-PII блоки роадмапа ВЫПОЛНЕНЫ (тесты 76→691, все зелёные). Сделано в этот заход: M5.2 (microEdit), M5.3 (microEditSchedule), M6.1 (resumeSuggestions, honesty-gated), M6.2 (resumeTailor, additive+дивергенция), M7.2 (daemonPlan), M7.3 (isolate), M9.2 (dailyReport). ОСТАТОК = строго execution-слой, требует подтверждения пользователя и/или живого HTML: M4.6 (реальная ОТПРАВКА ответов наружу), M4.7 (15-мин поллинг-демон), M5.1 (research-first: нативная кнопка hh.ru — нужен живой DOM/HTML), M5.4 (проводка микро-правок в цикл демона + ре-сейв реального resume.md), M6.3 (привязка тейлоринга к концу прогона — ЗАПИСЬ реального resume.md + бэкап), M7.4 (scripts/register-task.ps1 — Windows Task Scheduler, зависит от src/daemon.js), M7.5 (README+safety демона — документирует собранный демон, преждевременно до сборки). Чистые строительные блоки для ВСЕХ этих майлстоунов готовы и покрыты — остаётся только проводка/IO/исполнение наружу. M3-отсрочки (M3.1/3.3/3.5/3.8/3.11/3.12/3.14) — с обоснованием в ROADMAP.
2026-06-26 | M9.2 | DONE | src/lib/dailyReport.js (чистый дневной агрегатор, паттерн runSummary.js): createDailyReport() с методами recordAccountRun(name,snapshot) [суммирует applications-поля per-account snapshot, accountsCount=уникальные имена], recordMessages({processed,replied,skippedNoReply,manual}), recordResumeEdit({applied,addedSkillsCount}→editsApplied/editsSkipped), recordTokens(usageSnapshot) [имена сверены 1-в-1 с usageCounter.js: promptTokens/completionTokens/cacheHitTokens/calls], snapshot(date)→{date(YYYY-MM-DD UTC|null),accountsCount,accounts[],applications,messages,resume,tokens}, formatLine(). +dailyReportFileName(date)→'daily-YYYY-MM-DD.json' (throw на невалидном Date). Все record* молча игнорируют мусор (не бросают). Без IO — запись logs/daily-*.json = живой демон, за подтверждением. +40 тестов (агрегация/уникальность аккаунтов/мусор/имя файла/полный сценарий). npm test 691/691.
2026-06-26 | M7.3 | DONE | src/lib/isolate.js (чистый примитив изоляции ошибок демона): runIsolated(items,taskFn,opts) — последовательный прогон, КАЖДЫЙ элемент в try/catch, один сбой не прерывает остальные и НЕ пробрасывается наружу → {results[{item,index,ok,value|error}],succeeded,failed,total}; onError best-effort (сам бросит → проглот, логгер тоже изолирован); items не массив→[]; taskFn не функция→все ok:false без throw. runIsolatedTask(taskFn,...args)→{ok,value|error} для разовых шагов. isStopRequested({stopFileExists,signalReceived})→bool (чистое решение; IO STOP-файла/сигналов — снаружи). Без IO — реальный STOP-файл/обработчик сигналов/лог-на-итерацию = живой демон, за подтверждением. +42 теста (главный: sync throw одного не прерывает остальные; async reject; onError-бросок не роняет; порядок results; isStopRequested комбинации). npm test 651/651.
2026-06-26 | M7.2 | DONE | src/lib/daemonPlan.js (чистый планировщик решений демона, детерминирован от now/state/config): MESSAGES_POLL_MINUTES=15, MICRO_EDIT_MINUTES=30, ACTIONS (STOP/APPLY_RUN/POLL_MESSAGES/MICRO_EDIT/IDLE, frozen). decideNextAction({now,state,config}) приоритет: 1) вне рабочих часов МСК→STOP; 2) !startupDone→APPLY_RUN; 3) каденция сообщений настала→POLL_MESSAGES (приоритетнее, чувствительны ко времени); 4) каденция микро-правок→MICRO_EDIT; 5) иначе IDLE+nextCheckInMinutes (min до ближайшей каденции, >=0). Переиспользует isWithinWorkingHours (schedule.js) + canRunNow/minutesUntilAllowed (microEditSchedule.js). Guard now→TypeError; state/config не-объект→{}; каденции нормализуются. Без IO — реальный цикл src/daemon.js (исполнение наружу) за подтверждением. +30 тестов (приоритет, границы каденции включительно, кастом/мусор config, вне окна, невалидный now). npm test 609/609.
2026-06-26 | M6.2 | DONE | src/lib/resumeTailor.js (чистые функции диффа/лимита/additive-применения, без IO — реальная запись = M6.3): computeDivergence (построчно, мультимножество, Сёренсен–Дайс similarity, addedLines/removedLines/changedLines); withinDivergenceLimit (DEFAULT_MAX_CHANGED_LINES=8, MIN_SIMILARITY=0.85; нормализация лимитов); applyAdditiveSkills (ADDITIVE-ONLY, MAX_NEW_SKILLS=8; добавляет ТОЛЬКО навыки из явного approvedSkills, пропуск already_present/duplicate/empty/over_limit; вставка после заголовка навыков или новый блок в конце); tailorResume (оркестр + откат при превышении лимита → tailored===original, reason='divergence_limit_exceeded'); backupFileName(base,date) UTC timestamped (явный Date, throw на невалидном). HONESTY: модуль НИКОГДА не выдумывает навык — applied ⊆ approvedSkills; пустой approvedSkills → текст байт-в-байт. Self-review поймал и закрыл РЕАЛЬНЫЙ additive-баг: вставка по match[0].length искажала многословный заголовок («## Ключевые навыки и технологии») и заголовок в конце файла → переписано на построчную вставку (split/join, findIndex) — каждая исходная строка сохраняется как отдельный элемент. code-reviewer APPROVE w/nits → закрыты: M1 CRLF (детект доминирующего EOL, без смешанных переводов перед записью реального resume.md), L1 (комментарий про двойную present-проверку boundary-class). +47 тестов (дивергенция/лимиты/additive-инвариант/откат/backupFileName/многословный+конечный заголовок/CRLF). npm test 579/579.
2026-06-26 | M6.1 | DONE | src/lib/resumeSuggestions.js (чистый машинно-применимый слой над summary из buildResumeUpgradeReport): buildResumeSuggestions({summary,resumeText,skillsLimit,minFrequency}) → {vacanciesSeen,relevantVacancies,skillSuggestions[],alreadyPresent[]}. ТОЛЬКО additive: предлагает навыки, которых НЕТ в резюме, frequency>=minFreq(2), отсортированы freq DESC/name ASC, обрезаны skillsLimit(30). Honesty: каждое предложение несёт requiresRealExperience:true + justification со ссылкой на частоту; модуль НИКОГДА не утверждает наличие навыка. summarizeSuggestions для лога. code-reviewer REQUEST-CHANGES (High H1/H2: present-set строился ТОЛЬКО из whitelist RESUME_KEYWORDS → навык вне whitelist, напр. Jenkins, или whitelist-навык с хвостовой точкой «Helm.» ошибочно предлагался как новый = нарушение additive на пути к правке реального резюме) → ЗАКРЫТО: добавлен isSkillInResumeText (boundary-матч по сырому тексту резюме, генеративно-безопасный: при сомнении=присутствует→не предлагаем); + floor частоты (M2), localeCompare пин 'en'/base (L1), dedup-Map с max-частотой независимо от порядка входа (L2), JSDoc alreadyPresent=информационный (M1). +6 honesty-тестов (Jenkins вне whitelist, Helm., Go≠Google, dedup-max, floor). НЕ во флоу (проводка применения — M6.2/M6.3, за подтверждением). npm test 533/533.
2026-06-26 | M5.3 | DONE | src/lib/microEditSchedule.js (чистый rate-limiter, детерминирован от входного Date): DEFAULT_CADENCE_MINUTES=30/COOLDOWN=0; effectiveIntervalMinutes=max(cadence,cooldown) с нормализацией (cadence<=0/нечисло→дефолт; cooldown<0/нечисло→0, ноль допустим); nextAllowedAt=last+interval; canRunNow (lastRunAt null/undefined→true «никогда не запускались»; граница включительна now>=next; не-Date→TypeError); minutesUntilAllowed (null→0, уже можно→0, дробные не округляются). Guard Date как schedule.js. Без IO — проводка в 30-мин цикл демона M5.4 (за подтверждением). +50 тестов (фиксированные даты, нормализация мусора, границы, cooldown строже cadence). npm test 497/497.
2026-06-26 | M5.2 | DONE | src/lib/microEdit.js (чистые: canonical=trimEnd «снимок-эталон»; applyMicroEdit=canonical+MARKER('\n'); revert=canonical). Маркер = один хвостовой '\n': семантически незначим, надёжно снимается trimEnd, дифф ровно +1 симв., полностью обратим. Инварианты покрыты: revert(applyMicroEdit(t))===canonical(t), canonical(applyMicroEdit(t))===canonical(t) (смысл сохранён), applyMicroEdit(t)!==canonical(t) (правка реальна), startsWith. Guard не-строки→''. Без IO/сети — реальный resume.md не трогается (проводка ре-сейва — M5.4, за подтверждением). +35 тестов (canonical/applyMicroEdit/revert/инварианты/edge на markdown-резюме). npm test 447/447.
2026-06-26 | M7.1 | DONE | src/lib/schedule.js (чистые МСК-функции, детерминированы от входного Date, без Date.now()): WORK_START_HOUR=9/END=18, mskParts (через Intl Europe/Moscow), isWithinWorkingHours [9,18), nextRunAt (внутри окна→candidate; за окном вечером→завтра 09:00 МСК; ночь<09:00→сегодня 09:00 МСК; rollover месяца/года через UTC-арифметику+Intl). +30 тестов (граничные часы, 3 ветки, rollover мес/год, Infinity/NaN→0). code-reviewer APPROVE w/nits → закрыты: Number.isFinite guard (Infinity→0), доку pre-2014 офсет, rollover-тесты. npm test 417/417. (M7.2-M7.5: демон/автозапуск — граница «outbound/реальный запуск», за подтверждением.)
2026-06-26 | M10.2 | DONE | .github/workflows/ci.yml: на push (все ветки) + PR → checkout, setup-node@v4 (node 20, npm cache), npm ci, npm test. PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 (тесты грузят только чистые lib-модули, браузеры не нужны). package-lock.json в трекинге → npm ci/cache работают. YAML провалидирован парсером (jobs.test, 4 шага, node 20). M10 ЗАВЕРШЁН.
2026-06-26 | M10.1 | DONE | src/lib/validateConfig.js (чистая: {apiKey,resume,salary}→{ok,errors,warnings}; пустой ключ/резюме=errors, пустой salary=warning; не-строки=пустые, не бросает). Проводка в check.js: читает default resume/salary (локальная stripTemplate, без импорта review.js), apiKey из env, печатает факт наличия (НЕ сам ключ/содержимое), exit 1 при errors. +9 тестов. self-review оркестратора (чистый валидатор + диагностика, ключ/PII не логируются). npm test 387/387.
2026-06-26 | M9.1 | DONE | src/lib/runSummary.js (createRunSummary: record(result)→viewed+категории по status: applied/skipped/manual/alreadyApplied/dryRun/errors/quit + scoredBy→locallyScored/modelScored/cachedScored; snapshot/formatLine; не бросает на мусоре). config.getAccountSummaryPath. reviewVacancy помечает scoredBy (cache/local/model) в результатах после скоринга. processAccount собирает summary, пишет console + logs/summary-<account>.json (best-effort) с tokensRunCumulative (честно помечен глобальным — per-account атрибуции нет при параллели). .gitignore +logs/summary*.json. +20 тестов. self-review оркестратора (наблюдаемость, без outbound/секретов). npm test 378/378.
2026-06-26 | M8.3 | DONE | Аудит закрытия browser/context на всех путях. Найдена и закрыта РЕАЛЬНАЯ утечка в launchBrowser (browser.js): если newContext/newPage падает ПОСЛЕ успешного chromium.launch (напр. битый storageState), браузер осиротевал (вызов отклонялся до того, как caller получит browser → finally не закрыл) → теперь try/catch закрывает browser перед rethrow. Оба caller'а (processAccount, login.js) уже имели try/finally close (launchBrowser вне try — корректно); укреплён close → .catch(()=>{}) (вторичный бросок не маскирует исходный/не роняет другие аккаунты). launchBrowser не юнит-тестируется (chromium не мокается без DI) — acceptance=ревью+ручная, выполнено через аудит. npm test 358/358 (без новых; hardening).
2026-06-26 | M8.2 | DONE | --dry-run: src/lib/applyGuard.js (isSubmitAllowed → dryRun!==true, fail-safe) + флаг в parseArgs (дефолт false) + проводка в reviewVacancy/completeApplicationFlow. Главный гард ПОСЛЕ порога, ДО findResponseButton/клика/completeApplicationFlow → полный скоринг, 0 apply-действий; defense-in-depth в completeApplicationFlow. +6 тестов. code-reviewer APPROVE w/nits + ЯВНОЕ подтверждение: полная энумерация submit-сайтов (RESPONSE/APPLICATION_FLOW клики, selectFirstResumeOption, openCoverLetterEditor, form check/click) — НИ ОДИН не достижим в dry-run. parseArgs не юнит-тестируется (импорт-сайд-эффекты) — флаг проверен static-trace + node --check. npm test 358/358.
2026-06-26 | M8.1 | DONE | Единый реестр: REQUIRED_MANUAL_PATTERNS/RESPONSE_BUTTON_TEXTS/APPLICATION_FLOW_BUTTON_TEXTS перенесены байт-в-байт из review.js в src/lib/selectors.js (рядом с CHAT_SELECTORS из M4.1); APPLIED_PATTERNS re-export из applied.js (единый источник). review.js импортит из реестра, инлайн удалён, использования те же → флоу идентичен. +19 тестов (типы/количества 6/2/9/якорные матчи). self-review оркестратора (механическая экстракция, байт-в-байт). npm test 352/352.
2026-06-26 | M3 ИТОГ | NOTE | Доминирующая стоимость токенов (600 скоринг-вызовов) срезана: M3.2 локальный reject (0 токенов на junk), M3.9 усечение входа 9000→1500, M3.6 кэш (повтор=0), M3.15 RAG 3000→1200 + 0 для salary, M3.7 applied-skip, M3.13 опц.письма=0, M3.16 счётчик, M3.4 батч-блок для M7. ОТЛОЖЕНЫ с обоснованием в ROADMAP: M3.1 (нужен HTML карточек поиска), M3.3 (дорогой DOM-скан + маргинально под M3.2), M3.5 (overlap с M3.2 + конфликт с парал.аккаунтами), M3.8 (нужна company из DOM, узкий кейс над M3.6), M3.11 (риск на качестве откликов, мало вызовов), M3.12 (нужен живой API для замера), M3.14 (нет структурир. prefs → риск на откликах). M3-экономия по сути завершена.
2026-06-26 | M3.4 | DONE | src/lib/batchScore.js (buildBatchScoreMessages чистая + scoreVacanciesBatch через внедряемый callDeepSeek). Один вызов на N: модель просим {"results":[{id,score,reason}]} (обёртка под parseJsonObject), каждый кламп 0..100, выравнивание по id в порядке входа, missing→missing_in_batch (без aiFailed), extra id игнор, дубли last-wins. extractRequirements на каждую + resumeProfile (экономия входа). Деградация без throw: пусто→[], no_key→aiFailed, !ok→aiFailed(402/500), битый JSON→missing, отклонение callDeepSeek в try/catch. maxTokens=min(60N,1200), рекомендация N<=~20 в JSDoc. Standalone — НЕ во флоу (блок для демона M7). +33 теста (мок callDeepSeek). code-reviewer APPROVE w/nits → закрыты: 'id' in <примитив> TypeError (typeof-guard), await callDeepSeek в try/catch. npm test 333/333.
2026-06-26 | M3.1/M3.3 | DEFER | M3.1 пре-фильтр: collectFromSearch собирает только URL (без title/snippet карточек) → без сниппета схлопывается в M3.2; нужен HTML страницы поиска. M3.3 зелёные плашки: extractHhMatchSignals = полный DOM-скан на вакансию (дорого) + под инвариантом M3.2 не пропускает к отклику без модели → экономия маргинальна. Оба зафиксированы в ROADMAP с условиями разблокировки.
2026-06-26 | M3.16 | DONE | src/lib/usageCounter.js (createUsageCounter фабрика + синглтон runUsageCounter): record(usage) суммирует prompt/completion/cache-hit токены + calls, snapshot, formatSummary. callDeepSeek surface-ит usage из data.usage (аддитивно, потребители не задеты) и record-ит ТОЛЬКО на успехе. review.js печатает formatSummary в конце прогона. +16 тестов (deepseek.test.mjs: 5 deepEqual обновлены под usage:null + 1 новый usage-кейс). self-review оркестратора (аддитивно, без secret/honesty-влияния). npm test 301/301. ЗАМЕР: счётчик готов — фактические токены/cache-hit за прогон видны в summary; «до/после» снимать на живом прогоне (A/B против API в dev недоступен).
2026-06-26 | M3.15 | DONE | pickKnowledgeChunks: дефолт limit 3→2 + новый maxChunkChars=600 (усекает top-N после slice; <=0/нечисло → без усечения). Проводка: askDeepSeek и askDeepSeekChoice больше НЕ шлют RAG для salary (salary отвечает из salary.md); coverLetter по-прежнему без RAG; fillDeepSeekFormBatch на дефолт 2. RAG-вход было до 3×1000=3000 → стало до 2×600=1200 симв./вызов (~2.5×), salary-вызовы 0 RAG. +6 тестов. code-reviewer APPROVE w/nits (prompt-integrity подтверждена: coverLetter/salary-пути целы, empty-fallback цел) → закрыт нит (тест нечислового maxChunkChars). npm test 285/285.
2026-06-25 | M3.6 | DONE | src/lib/scoreCache.js (hashResume sha256[:16], cacheKey=normalizedURL|hash, get/setCached с клампом, load/saveCache best-effort) → data/score-cache.json (добавлен в .gitignore). Врезка в reviewVacancy: кэш-хит ДО скоринга (пропускает local+model, 0 токенов), кэш-сет ПОСЛЕ только при !aiFailed; естественная инвалидация (смена резюме→новый hash→miss). loadCache/saveCache на аккаунт, save в finally. В кэш только {score,reason}+нормализованный URL+хэш — НЕ резюме/зарплата/секреты. +29 тестов. code-reviewer APPROVE w/nits + security-reviewer PASS w/nits → закрыты: merge-on-save (параллельные аккаунты не теряют чужие ключи) + cacheKey throw-safety (битый URL→''). npm test 279/279.
2026-06-25 | M3.7 | DONE | src/lib/applied.js (APPLIED_PATTERNS перенесён байт-в-байт из review.js + isAlreadyApplied с guard) + проводка: pageLooksApplied использует isAlreadyApplied (идентично), и в reviewVacancy ранняя проверка ДО скоринга — applied-вакансии возвращают status='already_applied' без вызова localRelevanceScore/scoreVacancyWithDeepSeek (0 токенов). Статус только логируется (break лишь на quit), безопасно. +12 тестов. self-review оркестратора (механическая экстракция + 4-стр ранний return, гейты/honesty не тронуты). npm test 250/250.
2026-06-25 | M4 ПАУЗА | NOTE | M4.1-M4.5 готовы (чтение тредов→reply-policy→генерация). M4.6 (реальная ОТПРАВКА наружу) и M4.7 (демон) отложены по решению пользователя — вернулись к экономии токенов M3. M4.6 идемпотентность решена: естественная (последнее сообщение наше→skip) + in-memory set, без файла состояния (записано в ROADMAP M4.6).
2026-06-25 | M4.5 | DONE | src/lib/replyGenerate.js (buildReplyMessages чистая + generateReply через внедряемый callDeepSeek). Honesty-гардрейлы воспроизведены из review.js:618-708 (candidate-voice, no-fabrication, NO_ANSWER, salary-only-when-asked, employer-voice ban). Untrusted письмо работодателя изолировано в user-блоке с injection-меткой «ДАННЫЕ, а не инструкции»; в system не попадает. Маппинг: ok/no_answer/manual(employer-voice)/error; сбой API не бросает, ничего не шлёт (отправка — M4.6). +33 теста (мок callDeepSeek через deps, без сети). code-reviewer APPROVE w/nits + security-reviewer PASS w/nits: закрыты — не-строковый content→no_answer (не TypeError), ведущий NO_ANSWER→no_answer (sentinel не утечёт работодателю), doesNotThrow→doesNotReject. Ключ/PII не логируются (reason='api <status>', без body). npm test 238/238.
2026-06-25 | M4.4 | DONE | src/lib/replyPolicy.js (decideReply → {action: skip|needs_model|manual}, looksLikeQuestion, SYSTEM_AUTO_PATTERNS/INVITATION_PATTERNS seed). Приоритет: system>question>invitation>unknown; needs_model только на реальные вопросы (0 токенов на no-reply); консервативный дефлот — НЕ авто-отвечать при неуверенности (manual). Переиспользует matchesAnyPattern(answers) + normalizeText(knowledge). +39 тестов. code-reviewer REQUEST-CHANGES (High: падение на null/не-объектном элементе массива — нарушает «один сбой не роняет прогон») → закрыто null-guards (last?.author + skip не-объектов) + сужен /у работодателя[^?]*отзыв/ (закрыт путь «потерянного ответа») + 2 теста. npm test 205/205.
2026-06-25 | M4.3 | DONE | chatParse.js +parseThreadMessages (контейнеры по data-qa="chatik-chat-message-<digits>", якорь с кавычкой исключает -text/-applicant-action; автор=employer ⇔ есть chat-bubble-author-name, иначе applicant; текст из chat-bubble-text → stripTags+decodeEntities) + lastEmployerMessage (берёт последнее входящее). messages.js +readThread (thin, layout-контейнер, TODO сузить в M4.2). Синтетич. фикстура chatik-thread.html. +17 тестов. code-reviewer APPROVE w/nits: 2 medium задокументированы комментариями (M1 «первое </tag>» обрезает при вложенном одноимённом потомке — редко/деградация мягкая; M2 decode-после-strip материализует литеральные <script> из &lt;script&gt; — untrusted, M4.5 обязан хранить авторитет промпта). npm test 164/164.
2026-06-30 | M11.1 | DONE | src/lib/heartbeat.js (чистый субстрат наблюдаемости для панели M11): buildHeartbeat({task,account,phase,index,total,lastEvent,state,ts}) — toStr НЕ применяет String() к объектам (PII/структуры не протекают в текстовые поля, не-строка→''), index/total через toCount (конечное>=0→floor, иначе null), state дефолт 'ok', ts через toIso (Date/epoch ms/ISO-строка→ISO, невалид→null); НИКОГДА не бросает на мусоре. isStale(heartbeat,now,thresholdMs) консервативный (при сомнении=устарел): не-объект/null или без разбираемого ts→true, граница включительно-свежая (age===threshold→false), ts в будущем→false, невалидный thresholdMs→DEFAULT_STALE_THRESHOLD_MS(120000); now невалид→TypeError. Детерминирован (без Date.now()/argless new Date()/IO). +17 тестов. code-reviewer APPROVE w/nits (PII-защита/boundary/guards подтверждены) → закрыт нит: JSDoc now расширен до {Date|number|string} под фактическое поведение toMs. npm test 898/898.
2026-06-25 | M4.1+M4.2 | DONE | research-first по живому DOM «Сообщения»: чат hh.ru = cross-origin iframe chatik.hh.ru, всё на стабильных data-qa. src/lib/selectors.js (CHAT_SELECTORS + CHATIK_IFRAME_SELECTOR/URL_PATTERN), src/lib/chatParse.js (parseThreadList: regex по data-qa, unread ⇔ chatik-info-badges, count из бейджа), синтетич. PII-free фикстура test/fixtures/chatik-threadlist.html, скелет src/messages.js (getChatFrame/listThreads/processUnread, без top-level сайд-эффектов). M4.2 (различение unread/read на фикстуре) закрыт тем же парсером. implementer→code-reviewer APPROVE w/nits: закрыты M1 (счётчик сквозь пробелы/вложенность) и M2 (фрагмент ограничен </a>, хвостовой бейдж не поглощается) + entry-guard через fileURLToPath. +14 тестов. npm test 147/147. EVIDENCE-селекторы: chatik-checkbox-only-unread (фильтр), a[data-qa^=chatik-open-chat-<id>] href=/chat/<id> (тред), chatik-info-badges (unread-счётчик), chatik-chat-message-<id>(-text), chat-bubble-author-name (входящее⇒работодатель), chat-bubble-text>span.markdown>p (текст), chatik-header-vacancy-link (вакансия), textarea[chatik-new-message-text]+chatik-do-send-message (ответ).
2026-06-30 | M11.3 | DONE | src/lib/resourceSample.js (чистый семплер ресурсов для панели M11): sampleProcessResources({memoryUsage,cpuUsage,prevCpu,elapsedMs})→{rssMb,heapMb,cpuPercent} + formatResources. CPU% из кумулятивных снимков cpuUsage (мкс): busyMicros/(elapsedMs*10) == delta/(elapsedMs*1000мкс)*100; нет prevCpu/cpuUsage или elapsedMs<=0 → 0 (первый семпл честно 0); >100% на многоядерной не клампится (намеренно); убывающая/NaN-дельта → 0 (!(x>0)). nonNegative на каждый операнд (Infinity/NaN/отриц→0), байты→МБ round 0.1. НИКОГДА не бросает на мусоре, без IO/сети/Date.now()/реального process (DI). +11 тестов (инъецированные значения). code-reviewer APPROVE-w-nits (CPU-математика верна, все clamp/never-throw инварианты держатся и покрыты, нет secret/PII/network/Playwright) → закрыт нит: комментарий к !(x>0). node --check ок. npm test 903→914.
2026-06-30 | M11.4 | DONE | Семплинг ресурсов в демоне → logs/resources.jsonl. src/lib/resourceLog.js: фабрика createResourceLogger(deps) — тонкая best-effort IO-обёртка над чистым sampleProcessResources (M11.3); замыкание держит prevCpu/prevTs для дельты CPU между вызовами (process.cpuUsage кумулятивен); первый семпл cpuPercent=0 (elapsedMs=0/нет prevCpu); пишет одну JSONL-строку {ts(ISO),rssMb,heapMb,cpuPercent,openContexts}, openContexts нормализуется (Number.isFinite&&>=0?floor:0); mkdir recursive per-write; вся работа в try/catch→null (запись ресурсов НИКОГДА не роняет цикл демона). DI: appendFile/mkdir/filePath/memoryUsage/cpuUsage/now. config.js +resourcesLogPath=logs/resources.jsonl; .gitignore +logs/resources.jsonl (подтверждено git check-ignore). daemon.js: логгер создан до главного цикла, await resourceLogger({openContexts:0}) раз в итерацию ПОСЛЕ stop-check (на STOP не пишет) ДО decideNextAction; openContexts=0 т.к. прогоны открывают/закрывают свой браузер в finally (нет постоянных контекстов на границе итерации); runSingleTask(--task) не трогали. +13 тестов (happy/первый-CPU=0/второй-CPU>0 через замыкание/mkdir recursive/openContexts norm/appendFile+mkdir throw→null/ровно нужные ключи без секретов/ts=инъецированный now). implementer(Sonnet)→code-reviewer APPROVE (5 точек верифицированы: ordering prevCpu верен, best-effort, openContexts norm, sampling unreachable-when-stopped, style/PII/deps; только не-actionable ниты)→security-reviewer PASS (0 crit/high/medium: только числа, ключ/PII не уходят, путь git-ignored, нет сети, изоляция сбоя). node --check на resourceLog.js/config.js/daemon.js ок. npm test 914→927.
2026-06-30 | M11.2 | DONE | Heartbeat живого флоу в logs/status/<account>.json. src/lib/statusWriter.js: writeHeartbeatFile(account,fields,deps) — тонкая best-effort IO-обёртка над чистым buildHeartbeat (errors swallowed→null, DI для тестов, mkdir recursive per-write). config.js +statusDir/getAccountStatusPath +mkdir в ensureAppDirs; .gitignore +logs/status/. review.js пишет хартбит: старт цикла (index 0), после каждого результата (index+1, lastEvent=result.status — фикс-энам, без title/PII), error-ветка (lastEvent='error', НЕ error.message), финальный done. daemon.js: старт/done в runMessagesPass(messages) и runMicroEditPass(resume); apply пишет дочерний review.js. +5 тестов (statusWriter). code-reviewer APPROVE w/nits (best-effort изоляция реальна, PII clean, flow не тронут; нит: state всегда 'ok' — captcha/stalled придут в M11.6). security-reviewer PASS (0 crit/high: нет утечки ключа/PII, нет нового сетевого выхода, нет traversal — normalizeAccountName, logs/status/ git-ignored). node --check ок на review.js/daemon.js/config.js/statusWriter.js. npm test 903/903.
2026-06-30 | M11.6 | DONE | src/lib/runState.js (чистый детектор состояния прогона для панели M11): detectAntiBot(pageTextOrSignals) — строка ИЛИ сигнал-объект {text}/{captcha|antiBot:true}; матчит ANTIBOT_PATTERNS (русский UI hh.ru: «подтвердите/докажите, что вы не робот», «я не робот», captcha/капча, проверка безопасности, подозрит. активность); инцидент belonogov 2026-06-29 «Подтвердите, что вы не робот» покрыт. ВАЖНО: JS-regex без флага `u` → `\b`/`\w` ASCII-only, НЕ матчат на кириллической границе — первый прогон поймал баг (`\bкапч`/`подозрительн\w*` не срабатывали на кириллице), переписано на `капч[аеиныйу]`/`подозрительн\S*\s+активност` (единственный `\b` — в `/\bcaptcha\b/i`, ASCII, корректно). detectStalledRun(hb,now,thresholdMs,withinWorkingHours) — stalled только ВНУТРИ окна (вне окна простой ожидаем) И stale через heartbeat.isStale; withinWorkingHours строго ===true; невалидный now (isStale бросил бы TypeError) → false (не роняет цикл). resolveRunState({...}) → литерал 'captcha'|'stalled'|'ok' (приоритет captcha>stalled>ok). Все три never-throw на мусоре/null. Untrusted текст страницы только .test()-матчится против фикс-паттернов, наружу не возвращается/не становится селектором (prompt-injection-safe). Без IO/Date.now()/argless new Date() — проводка state→heartbeat.state + warn→logs/alerts.jsonl (через alerts.js) из живого review.js/daemon.js = gated follow-up (паттерн M11.1→M11.2, M11.3→M11.4). +20 тестов (текст/сигнал-объект/булев флаг/false-positive «роботов»/границы окна/невалидный now/приоритет/дефолт-порог). implementer-роль: оркестратор написал напрямую (фокусный pure-модуль) → code-reviewer APPROVE WITH NITS (0 blocking/should-fix: Cyrillic-фикс подтверждён, never-throw/семантика/приоритет/style/injection все clean; нит про дефолт-порог закрыт +тестом). node --check ок. npm test 932→952.
2026-06-30 | M11.5 | DONE | Живой счётчик токенов/стоимости «за сегодня». src/lib/usageCounter.js: новый liveSnapshot(pricing) на фабрике createUsageCounter (и автоматически на синглтоне runUsageCounter) → {...snapshot(), estimatedCostUsd}: те же счётчики (calls/prompt/completion/total/cacheHit/apiErrors/balanceExhausted), что snapshot(), плюс оценка $ через delegation в metrics.estimateCost (единый источник цен — без price-drift с dashboard.js, который зовёт ту же функцию). Снимок читается в ЛЮБОЙ момент прогона: не завершает прогон, не мутирует состояние (snapshot() уже иммутабелен), never-throws (estimateCost guard-ит каждое поле Number.isFinite, pricing={} дефолт). Поля snapshot() (promptTokens/completionTokens/cacheHitTokens) точно совпадают с входом estimateCost; лишние ключи игнорируются. metrics.js без import → нет циклической зависимости (usageCounter→metrics односторонняя). +5 тестов (нули→$0; все поля присутствуют; делегирование к estimateCost вместо дублирования магических цен; custom pricing; идемпотентность чтения). code-reviewer APPROVE (0 blocking/should-fix; нит про дублирование цен в тесте — закрыт: тест теперь сверяется с estimateCost(snapshot()), не с хардкодом). node --check usageCounter.js ок. npm test 927→932.
2026-06-30 | M11.7 | DONE | src/lib/taskControl.js (buildTaskCommand + canStart) + test/taskControl.test.mjs (65). buildTaskCommand: алиасы task как в daemonArgs, --live ТОЛЬКО при live===true (safety-инвариант, тест на truthy-не-true), limit/text/area только для apply (limit→floor целого), бросает на плохой task/account. canStart: одна задача на аккаунт, защита от не-массива/мусорных записей/плохого account/task. implementer(Sonnet)→code-reviewer(APPROVE w/nits: argv верифицирован round-trip через parseDaemonArgs/daemon.js; нит truthy-live + integer-limit применены). npm test 1015/1015 зелёных.
